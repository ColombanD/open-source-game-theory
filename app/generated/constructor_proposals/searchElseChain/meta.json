{
  "name": "searchElseChain",
  "date": "2026-08-03",
  "unblocks": "llm_outcome_OptimBot_vs_DefectBot at staggered budgets (kSelf >> kOpp) -> (D, D) — the flip outcome_status.toml records as expected — plus OptimBot vs TitForTatBot and OptimBot vs OBot by the same skeleton. The rule is the search_f twin of searchChain: a mixed-polarity search-telescope reading rule whose else-layers carry Σ₁ refutation antecedents (.neg of the crossed guard) and pay the full failed budget (the search_f floor, forced by provable-soundness). OptimBot's defection vs DefectBot sits behind two refuted cooperation rungs (else-else-then-then); no current rule internalizes a play behind a FAILED search under a box hypothesis, so the Löb premise box_{kSelf}(phiD) -> phiD is underivable: searchChain/ctxChain descend then-slots only and their sole decomposition carries the unprovable-false antecedent box_{kOpp}(DefectBot plays C); weakenImpl needs the consequent whose atom certificate regresses through search_t on the self-guard; searchThenSearch_t needs const-const inner branches. unblocking_demo.lean (compiled, rule as hypothesis with its cost condition) derives the exact bloeb_engine premise at 2*kOpp + O(log kOpp + log kSelf); soundness_certificate.lean (compiled) proves the rule's interp content via the mixed-polarity eval induction plug2_eval. Does NOT unblock the uniform-budget negative direction (the OptimBot-falls-to-C censuses), which remains behind the implTrans/modal-tier wall.",
  "status": "integrated",
  "integrated_as": "Pf.searchElseChain",
  "integration_date": "2026-08-04",
  "integration_note": "Integrated interactively (human-accepted in-session; the app worktree flow was not used). Landed with ONE refinement over the filed rule: elseL layers store their guards as PLAYS-SHAPED components (elseL g P Q c p, guard .plays P Q c) rather than an arbitrary Formula — structurally, like CtxLayer.iteL. This is what lets the master valuation-soundness lemma (wv_sound_upto) discharge the census motive with NO interface change (WV-falsity of a plays-atom implies interp-falsity); every zoo else-crossing has a plays guard. Full blast radius landed: Pf_mono/Pf.induct wiring, wv_sound_upto arm (plug2_eval), the exclusion kernel's 11th obligation (k < layersCost for any plug2 decomposition) with all ~30 census call sites discharged, pf_size_or_atom/tail_plays_readable arms, and the FULL Metatheory mirror: PfG.searchElseChain (ungated leaf), the verified enumerator's elseChainGo parser + chkSearchElseChain with soundness/completeness, decB/decFull completeness arms, the T49 substrate node (excluded from the machine-walkable dbFree fragment like searchChain), T50 transport, T51 falsification repair, T52 instance decider. Both lake targets green (3314 jobs). First unblocked outcome LANDED: llm_outcome_OptimBot_vs_DefectBot = (D,D) at stagger kSelf = 65536*kOpp (Theorems/OptimBot/vs_DefectBot.lean); outcome_status.toml entry dropped. OptimBot vs TitForTatBot and vs OBot staggered (D,D) are now derivable by the same skeleton."
}