"""API entrypoint scaffold.

Install optional dependencies and replace this with a concrete FastAPI app:
`pip install -e .[api]`
"""

from __future__ import annotations


def create_app() -> object:
    # Placeholder factory for API app object.
    return object()
