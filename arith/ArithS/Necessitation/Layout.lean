import ArithS.Necessitation.Describe
import ArithS.Necessitation.RowInstB

/-!
# ArithS.Necessitation.Layout — the three layout producers: copy-in, chains, identification

`M4_BOUNDED_HBL/DESIGN_fragments.md` §3.3–3.5, as Σ₁ step-list producers against the step
language of `Chain.lean` and the list infrastructure of `Describe.lean` Part 0, each with its
Π₁ applicability theorem in the `describeT_ok` shape (`ListOK ∧ NoDrop ∧ HornOnly ∧ shiftsV = count
∧ the produced facts ∈ finalCtx`, facts stated as canonical codes at explicit indices) and its
length; the cost follows from `costSum_le_of_hornOnly` (every list here is Horn-only).

## Part 1 — the layout table (GENERATED by the session script `gen_layout1.py`)

`layoutRows := walkRows ++ layoutExtraRows` — the 40 walk rows at their indices followed by the
46 rows the producers use (copy-in `eqTotal`/`eqRefl`/`eqSymm`/`eqTrans`/`cong*`, identification
`eqOf*`, `subsetAntisymm`, the chain rows `*C` of `Lib/Frag.lean` group K), `lIdx_<row> = 40 + k`;
`LayoutTable tbl` (implies `WalkTable tbl`), `exists_layoutTable`; the piece table `layoutPieces`
extends `walkPieces` entrywise (`mkStep_layoutPieces_lt`), so the walk's `ok_<row>` lemmas
transfer; per row `lmk_<row>`, `layoutTable_<row>`, `ltag_<row>`, `lok_<row>`.

## Part 2 — copy-in (`copySteps`, §3.3)

A FACT TAG `⟪kind, args⟫` names one fact of the source object's dossier by its kind (`0` pi, `1`
and, `2` or, `3` all, `4` exs, `5` rel, `6` nrel, `7` verum, `8` falsum, `9` len, `10` mem, `11`
tpi, `12` func, `13` bvar, `14` fvar, `15` tlen, `16` adj, `17` tvpi, `18` utvpi) and the OTHER
witnesses of that fact (closed terms in the frame BEFORE the copy; for the shape facts also the
NEW children, whose identification `eqFactB child' child` is assumed in context). `copySteps W i T`
= `sIntroFact eqTotal [^&i]` (the fresh `&0` with `eqFactB &0 &(i+1)`, everything shifted once)
followed, per tag, by the congruence step(s) (`cong<kind>`, plus the `.sigma → .pi` bridge row of
the walk for the four formation kinds). `copySteps_ok`: under `TagOK` for every tag (kind `< 19`,
witnesses closed with shifted length `≤ E`, the tag's antecedents in `Γ`), the list is applicable,
Horn-only, shifts exactly once, and its final context holds `eqFactB &0 &(i+1)` and every
`tagFact` — the copied fact about `&0` with the witnesses shifted.

## Part 3 — chains (`chainSteps`, §3.4)

`chainSteps W xs` for a vector of member terms (frame before the chain): `k` intro steps
`insertTotalC` building `s₁ = insert x₁ 𝟎, …, s_k` (so `s_j` ends at `&(k − j)`), then the
membership facts in one backward pass (`subsetInsertC`/`subsetTransC` for `s_j ⊆ s_k`,
`memInsertSelfC` + `subsetMemC`), the formula-set facts (`emptySubsetC` + `fsetOfSubsetZeroC` for
`𝟎`, then `isFormulaSetInsertC` + `fsetSigmaPiC` per link, needing `piFact 𝟎 x_j` from the dossiers),
and finally `setLenTotalC` for the length object `l_s = &0` (every chain object moves up by one).

## Part 4 — identification (`eqSteps`, §3.5)

Two walk-style dossiers of the SAME code `r` at offsets `i` and `j` (the layout convention: the top
at the offset, the descendants at the walk's relative offsets) yield `eqFactB &i &j` bottom-up by
the injectivity rows `eqOf*` (`eqRefl` on the literal `𝟎` for empty vectors). A `Fixpoint` on
`⟪r, i, j⟫` over the formula/vector/term kinds is the design's walk order; this file delivers the
producer relative to an explicit dossier predicate (see the Part 4 docstring for what is assumed).
-/

namespace ArithS

open FFL FFL.FirstOrder Arithmetic Bootstrapping
open PeanoMinus ISigma0 ISigma1
open LAct

variable {V : Type*} [ORingStructure V] [V↓[ℒₒᵣ] ⊧* 𝗜𝚺₁]

/-! ## Part 1 — the layout table: the walk rows followed by the copy/identification/chain rows (GENERATED)

`layoutRows := walkRows ++ layoutExtraRows` (indices `lIdx_<row> = 40 + k`), `LayoutTable tbl`
(which implies `WalkTable tbl`), `exists_layoutTable`, the piece table `layoutPieces` (extending
`walkPieces` entrywise, so `mkStep layoutPieces i ev = mkStep walkPieces i ev` for `i < 40` and the
walk's `ok_<row>` lemmas transfer), and per row `lmk_<row>`, `layoutTable_<row>`, `ltag_<row>`,
`lok_<row>` (the step is applicable and its `ctxAfter` inserts the canonical fact). -/

section layoutTable

def lIdx_eqTotal : ℕ := 40
def lIdx_eqRefl : ℕ := 41
def lIdx_eqSymm : ℕ := 42
def lIdx_eqTrans : ℕ := 43
def lIdx_congPi : ℕ := 44
def lIdx_congTPi : ℕ := 45
def lIdx_congTvPi : ℕ := 46
def lIdx_congUtvPi : ℕ := 47
def lIdx_congAnd : ℕ := 48
def lIdx_congOr : ℕ := 49
def lIdx_congAll : ℕ := 50
def lIdx_congExs : ℕ := 51
def lIdx_congRel : ℕ := 52
def lIdx_congNRel : ℕ := 53
def lIdx_congVerum : ℕ := 54
def lIdx_congFalsum : ℕ := 55
def lIdx_congFunc : ℕ := 56
def lIdx_congBvar : ℕ := 57
def lIdx_congFvar : ℕ := 58
def lIdx_congAdj : ℕ := 59
def lIdx_congLen : ℕ := 60
def lIdx_congTLen : ℕ := 61
def lIdx_congMem : ℕ := 62
def lIdx_eqOfAnd : ℕ := 63
def lIdx_eqOfOr : ℕ := 64
def lIdx_eqOfAll : ℕ := 65
def lIdx_eqOfExs : ℕ := 66
def lIdx_eqOfRel : ℕ := 67
def lIdx_eqOfNRel : ℕ := 68
def lIdx_eqOfVerum : ℕ := 69
def lIdx_eqOfFalsum : ℕ := 70
def lIdx_eqOfFunc : ℕ := 71
def lIdx_eqOfBvar : ℕ := 72
def lIdx_eqOfFvar : ℕ := 73
def lIdx_eqOfAdj : ℕ := 74
def lIdx_subsetAntisymm : ℕ := 75
def lIdx_insertTotalC : ℕ := 76
def lIdx_memInsertSelfC : ℕ := 77
def lIdx_subsetInsertC : ℕ := 78
def lIdx_subsetTransC : ℕ := 79
def lIdx_subsetMemC : ℕ := 80
def lIdx_emptySubsetC : ℕ := 81
def lIdx_fsetOfSubsetZeroC : ℕ := 82
def lIdx_isFormulaSetInsertC : ℕ := 83
def lIdx_fsetSigmaPiC : ℕ := 84
def lIdx_setLenTotalC : ℕ := 85
def layoutRowCount : ℕ := 86

/-- The rows beyond the walk's 40, in index order. -/
noncomputable def layoutExtraRows : List WRow := [
  ⟨1, eqTotalB, lib_eqTotal⟩,
  ⟨1, eqReflB, lib_eqRefl⟩,
  ⟨2, eqSymmB, lib_eqSymm⟩,
  ⟨3, eqTransB, lib_eqTrans⟩,
  ⟨3, congPiB, lib_congPi⟩,
  ⟨3, congTPiB, lib_congTPi⟩,
  ⟨4, congTvPiB, lib_congTvPi⟩,
  ⟨3, congUtvPiB, lib_congUtvPi⟩,
  ⟨6, congAndB, lib_congAnd⟩,
  ⟨6, congOrB, lib_congOr⟩,
  ⟨4, congAllB, lib_congAll⟩,
  ⟨4, congExsB, lib_congExs⟩,
  ⟨6, congRelB, lib_congRel⟩,
  ⟨6, congNRelB, lib_congNRel⟩,
  ⟨2, congVerumB, lib_congVerum⟩,
  ⟨2, congFalsumB, lib_congFalsum⟩,
  ⟨6, congFuncB, lib_congFunc⟩,
  ⟨3, congBvarB, lib_congBvar⟩,
  ⟨3, congFvarB, lib_congFvar⟩,
  ⟨6, congAdjB, lib_congAdj⟩,
  ⟨3, congLenB, lib_congLen⟩,
  ⟨3, congTLenB, lib_congTLen⟩,
  ⟨3, congMemB, lib_congMem⟩,
  ⟨6, eqOfAndB, lib_eqOfAnd⟩,
  ⟨6, eqOfOrB, lib_eqOfOr⟩,
  ⟨4, eqOfAllB, lib_eqOfAll⟩,
  ⟨4, eqOfExsB, lib_eqOfExs⟩,
  ⟨6, eqOfRelB, lib_eqOfRel⟩,
  ⟨6, eqOfNRelB, lib_eqOfNRel⟩,
  ⟨2, eqOfVerumB, lib_eqOfVerum⟩,
  ⟨2, eqOfFalsumB, lib_eqOfFalsum⟩,
  ⟨6, eqOfFuncB, lib_eqOfFunc⟩,
  ⟨3, eqOfBvarB, lib_eqOfBvar⟩,
  ⟨3, eqOfFvarB, lib_eqOfFvar⟩,
  ⟨6, eqOfAdjB, lib_eqOfAdj⟩,
  ⟨2, subsetAntisymmB, lib_subsetAntisymm⟩,
  ⟨2, insertTotalCB, lib_insertTotalC⟩,
  ⟨3, memInsertSelfCB, lib_memInsertSelfC⟩,
  ⟨3, subsetInsertCB, lib_subsetInsertC⟩,
  ⟨3, subsetTransCB, lib_subsetTransC⟩,
  ⟨3, subsetMemCB, lib_subsetMemC⟩,
  ⟨1, emptySubsetCB, lib_emptySubsetC⟩,
  ⟨1, fsetOfSubsetZeroCB, lib_fsetOfSubsetZeroC⟩,
  ⟨3, isFormulaSetInsertCB, lib_isFormulaSetInsertC⟩,
  ⟨1, fsetSigmaPiCB, lib_fsetSigmaPiC⟩,
  ⟨1, setLenTotalCB, lib_setLenTotalC⟩
]

/-- The layout table's rows: the walk's followed by the extra rows. -/
noncomputable def layoutRows : List WRow := walkRows ++ layoutExtraRows

lemma layoutRows_length : layoutRows.length = layoutRowCount := rfl

/-- **The layout proof table**: `layoutRowCount` rows at least, the `i`-th with the arity and the matrix
of `layoutRows[i]`. -/
def LayoutTable (tbl : V) : Prop :=
  (layoutRowCount : V) ≤ len tbl ∧
  ∀ (i : ℕ) (h : i < layoutRows.length),
    rowM tbl.[(i : V)] = ((layoutRows[i]).m : V) ∧ rowB tbl.[(i : V)] = ⌜Semiformula.lMap emb (layoutRows[i]).B⌝

/-- A layout table is a walk table (the first 40 rows). -/
lemma LayoutTable.walkTable {tbl : V} (h : LayoutTable tbl) : WalkTable tbl := by
  refine ⟨le_trans (by exact_mod_cast (by decide : walkRowCount ≤ layoutRowCount)) h.1, ?_⟩
  intro i hi
  have hi' : i < layoutRows.length := by
    rw [layoutRows_length]; exact lt_of_lt_of_le (walkRows_length ▸ hi) (by decide)
  have := h.2 i hi'
  rwa [show layoutRows[i] = walkRows[i] from List.getElem_append_left hi] at this

/-- **The layout proof table exists in every model, with one standard length bound.** -/
theorem exists_layoutTable : ∃ N : ℕ, ∀ (V : Type) [ORingStructure V] [V↓[ℒₒᵣ] ⊧* 𝗜𝚺₁],
    ∃ tbl : V, TableOK tbl (N : V) ∧ LayoutTable tbl := by
  obtain ⟨N, hN⟩ := exists_rows layoutRows
  refine ⟨N, fun V _ _ ↦ ?_⟩
  obtain ⟨rows, hlen, hok, hidx⟩ := hN V
  refine ⟨vecOf rows, tableOK_vecOf rows hok, ?_, ?_⟩
  · rw [len_vecOf, hlen, layoutRows_length]
  · intro i h
    have h' : i < rows.length := by rw [hlen]; exact h
    rw [nth_vecOf rows i h']
    exact hidx i h h'

lemma layoutTable_len {tbl : V} (h : LayoutTable tbl) (i : ℕ) (hi : i < layoutRowCount) : ((i : ℕ) : V) < len tbl :=
  lt_of_lt_of_le (by exact_mod_cast hi) h.1

/-! ### The per-row readings of `LayoutTable` -/

lemma layoutTable_eqTotal {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqTotal : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqTotal : ℕ) : V)] = impChainV LAct (vecOf row_eqTotal_as) (^∃ row_eqTotal_R) := by
  have this : rowM tbl.[((lIdx_eqTotal : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqTotal : ℕ) : V)] = ⌜Semiformula.lMap emb eqTotalB⌝ :=
    h.2 lIdx_eqTotal (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqTotal]
  rfl

lemma layoutTable_eqRefl {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqRefl : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqRefl : ℕ) : V)] = impChainV LAct (vecOf row_eqRefl_as) row_eqRefl_c := by
  have this : rowM tbl.[((lIdx_eqRefl : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqRefl : ℕ) : V)] = ⌜Semiformula.lMap emb eqReflB⌝ :=
    h.2 lIdx_eqRefl (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqRefl]

lemma layoutTable_eqSymm {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqSymm : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqSymm : ℕ) : V)] = impChainV LAct (vecOf row_eqSymm_as) row_eqSymm_c := by
  have this : rowM tbl.[((lIdx_eqSymm : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqSymm : ℕ) : V)] = ⌜Semiformula.lMap emb eqSymmB⌝ :=
    h.2 lIdx_eqSymm (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqSymm]

lemma layoutTable_eqTrans {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqTrans : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqTrans : ℕ) : V)] = impChainV LAct (vecOf row_eqTrans_as) row_eqTrans_c := by
  have this : rowM tbl.[((lIdx_eqTrans : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqTrans : ℕ) : V)] = ⌜Semiformula.lMap emb eqTransB⌝ :=
    h.2 lIdx_eqTrans (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqTrans]

lemma layoutTable_congPi {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congPi : ℕ) : V)] = impChainV LAct (vecOf row_congPi_as) row_congPi_c := by
  have this : rowM tbl.[((lIdx_congPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congPi : ℕ) : V)] = ⌜Semiformula.lMap emb congPiB⌝ :=
    h.2 lIdx_congPi (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congPi]

lemma layoutTable_congTPi {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congTPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congTPi : ℕ) : V)] = impChainV LAct (vecOf row_congTPi_as) row_congTPi_c := by
  have this : rowM tbl.[((lIdx_congTPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congTPi : ℕ) : V)] = ⌜Semiformula.lMap emb congTPiB⌝ :=
    h.2 lIdx_congTPi (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congTPi]

lemma layoutTable_congTvPi {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congTvPi : ℕ) : V)] = ((4 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congTvPi : ℕ) : V)] = impChainV LAct (vecOf row_congTvPi_as) row_congTvPi_c := by
  have this : rowM tbl.[((lIdx_congTvPi : ℕ) : V)] = ((4 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congTvPi : ℕ) : V)] = ⌜Semiformula.lMap emb congTvPiB⌝ :=
    h.2 lIdx_congTvPi (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congTvPi]

lemma layoutTable_congUtvPi {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congUtvPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congUtvPi : ℕ) : V)] = impChainV LAct (vecOf row_congUtvPi_as) row_congUtvPi_c := by
  have this : rowM tbl.[((lIdx_congUtvPi : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congUtvPi : ℕ) : V)] = ⌜Semiformula.lMap emb congUtvPiB⌝ :=
    h.2 lIdx_congUtvPi (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congUtvPi]

lemma layoutTable_congAnd {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congAnd : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congAnd : ℕ) : V)] = impChainV LAct (vecOf row_congAnd_as) row_congAnd_c := by
  have this : rowM tbl.[((lIdx_congAnd : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congAnd : ℕ) : V)] = ⌜Semiformula.lMap emb congAndB⌝ :=
    h.2 lIdx_congAnd (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congAnd]

lemma layoutTable_congOr {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congOr : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congOr : ℕ) : V)] = impChainV LAct (vecOf row_congOr_as) row_congOr_c := by
  have this : rowM tbl.[((lIdx_congOr : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congOr : ℕ) : V)] = ⌜Semiformula.lMap emb congOrB⌝ :=
    h.2 lIdx_congOr (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congOr]

lemma layoutTable_congAll {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congAll : ℕ) : V)] = ((4 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congAll : ℕ) : V)] = impChainV LAct (vecOf row_congAll_as) row_congAll_c := by
  have this : rowM tbl.[((lIdx_congAll : ℕ) : V)] = ((4 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congAll : ℕ) : V)] = ⌜Semiformula.lMap emb congAllB⌝ :=
    h.2 lIdx_congAll (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congAll]

lemma layoutTable_congExs {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congExs : ℕ) : V)] = ((4 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congExs : ℕ) : V)] = impChainV LAct (vecOf row_congExs_as) row_congExs_c := by
  have this : rowM tbl.[((lIdx_congExs : ℕ) : V)] = ((4 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congExs : ℕ) : V)] = ⌜Semiformula.lMap emb congExsB⌝ :=
    h.2 lIdx_congExs (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congExs]

lemma layoutTable_congRel {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congRel : ℕ) : V)] = impChainV LAct (vecOf row_congRel_as) row_congRel_c := by
  have this : rowM tbl.[((lIdx_congRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congRel : ℕ) : V)] = ⌜Semiformula.lMap emb congRelB⌝ :=
    h.2 lIdx_congRel (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congRel]

lemma layoutTable_congNRel {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congNRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congNRel : ℕ) : V)] = impChainV LAct (vecOf row_congNRel_as) row_congNRel_c := by
  have this : rowM tbl.[((lIdx_congNRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congNRel : ℕ) : V)] = ⌜Semiformula.lMap emb congNRelB⌝ :=
    h.2 lIdx_congNRel (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congNRel]

lemma layoutTable_congVerum {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congVerum : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congVerum : ℕ) : V)] = impChainV LAct (vecOf row_congVerum_as) row_congVerum_c := by
  have this : rowM tbl.[((lIdx_congVerum : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congVerum : ℕ) : V)] = ⌜Semiformula.lMap emb congVerumB⌝ :=
    h.2 lIdx_congVerum (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congVerum]

lemma layoutTable_congFalsum {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congFalsum : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congFalsum : ℕ) : V)] = impChainV LAct (vecOf row_congFalsum_as) row_congFalsum_c := by
  have this : rowM tbl.[((lIdx_congFalsum : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congFalsum : ℕ) : V)] = ⌜Semiformula.lMap emb congFalsumB⌝ :=
    h.2 lIdx_congFalsum (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congFalsum]

lemma layoutTable_congFunc {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congFunc : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congFunc : ℕ) : V)] = impChainV LAct (vecOf row_congFunc_as) row_congFunc_c := by
  have this : rowM tbl.[((lIdx_congFunc : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congFunc : ℕ) : V)] = ⌜Semiformula.lMap emb congFuncB⌝ :=
    h.2 lIdx_congFunc (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congFunc]

lemma layoutTable_congBvar {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congBvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congBvar : ℕ) : V)] = impChainV LAct (vecOf row_congBvar_as) row_congBvar_c := by
  have this : rowM tbl.[((lIdx_congBvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congBvar : ℕ) : V)] = ⌜Semiformula.lMap emb congBvarB⌝ :=
    h.2 lIdx_congBvar (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congBvar]

lemma layoutTable_congFvar {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congFvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congFvar : ℕ) : V)] = impChainV LAct (vecOf row_congFvar_as) row_congFvar_c := by
  have this : rowM tbl.[((lIdx_congFvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congFvar : ℕ) : V)] = ⌜Semiformula.lMap emb congFvarB⌝ :=
    h.2 lIdx_congFvar (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congFvar]

lemma layoutTable_congAdj {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congAdj : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congAdj : ℕ) : V)] = impChainV LAct (vecOf row_congAdj_as) row_congAdj_c := by
  have this : rowM tbl.[((lIdx_congAdj : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congAdj : ℕ) : V)] = ⌜Semiformula.lMap emb congAdjB⌝ :=
    h.2 lIdx_congAdj (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congAdj]

lemma layoutTable_congLen {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congLen : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congLen : ℕ) : V)] = impChainV LAct (vecOf row_congLen_as) row_congLen_c := by
  have this : rowM tbl.[((lIdx_congLen : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congLen : ℕ) : V)] = ⌜Semiformula.lMap emb congLenB⌝ :=
    h.2 lIdx_congLen (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congLen]

lemma layoutTable_congTLen {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congTLen : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congTLen : ℕ) : V)] = impChainV LAct (vecOf row_congTLen_as) row_congTLen_c := by
  have this : rowM tbl.[((lIdx_congTLen : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congTLen : ℕ) : V)] = ⌜Semiformula.lMap emb congTLenB⌝ :=
    h.2 lIdx_congTLen (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congTLen]

lemma layoutTable_congMem {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_congMem : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_congMem : ℕ) : V)] = impChainV LAct (vecOf row_congMem_as) row_congMem_c := by
  have this : rowM tbl.[((lIdx_congMem : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_congMem : ℕ) : V)] = ⌜Semiformula.lMap emb congMemB⌝ :=
    h.2 lIdx_congMem (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_congMem]

lemma layoutTable_eqOfAnd {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfAnd : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfAnd : ℕ) : V)] = impChainV LAct (vecOf row_eqOfAnd_as) row_eqOfAnd_c := by
  have this : rowM tbl.[((lIdx_eqOfAnd : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfAnd : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfAndB⌝ :=
    h.2 lIdx_eqOfAnd (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfAnd]

lemma layoutTable_eqOfOr {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfOr : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfOr : ℕ) : V)] = impChainV LAct (vecOf row_eqOfOr_as) row_eqOfOr_c := by
  have this : rowM tbl.[((lIdx_eqOfOr : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfOr : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfOrB⌝ :=
    h.2 lIdx_eqOfOr (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfOr]

lemma layoutTable_eqOfAll {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfAll : ℕ) : V)] = ((4 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfAll : ℕ) : V)] = impChainV LAct (vecOf row_eqOfAll_as) row_eqOfAll_c := by
  have this : rowM tbl.[((lIdx_eqOfAll : ℕ) : V)] = ((4 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfAll : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfAllB⌝ :=
    h.2 lIdx_eqOfAll (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfAll]

lemma layoutTable_eqOfExs {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfExs : ℕ) : V)] = ((4 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfExs : ℕ) : V)] = impChainV LAct (vecOf row_eqOfExs_as) row_eqOfExs_c := by
  have this : rowM tbl.[((lIdx_eqOfExs : ℕ) : V)] = ((4 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfExs : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfExsB⌝ :=
    h.2 lIdx_eqOfExs (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfExs]

lemma layoutTable_eqOfRel {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfRel : ℕ) : V)] = impChainV LAct (vecOf row_eqOfRel_as) row_eqOfRel_c := by
  have this : rowM tbl.[((lIdx_eqOfRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfRel : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfRelB⌝ :=
    h.2 lIdx_eqOfRel (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfRel]

lemma layoutTable_eqOfNRel {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfNRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfNRel : ℕ) : V)] = impChainV LAct (vecOf row_eqOfNRel_as) row_eqOfNRel_c := by
  have this : rowM tbl.[((lIdx_eqOfNRel : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfNRel : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfNRelB⌝ :=
    h.2 lIdx_eqOfNRel (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfNRel]

lemma layoutTable_eqOfVerum {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfVerum : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfVerum : ℕ) : V)] = impChainV LAct (vecOf row_eqOfVerum_as) row_eqOfVerum_c := by
  have this : rowM tbl.[((lIdx_eqOfVerum : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfVerum : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfVerumB⌝ :=
    h.2 lIdx_eqOfVerum (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfVerum]

lemma layoutTable_eqOfFalsum {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfFalsum : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfFalsum : ℕ) : V)] = impChainV LAct (vecOf row_eqOfFalsum_as) row_eqOfFalsum_c := by
  have this : rowM tbl.[((lIdx_eqOfFalsum : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfFalsum : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfFalsumB⌝ :=
    h.2 lIdx_eqOfFalsum (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfFalsum]

lemma layoutTable_eqOfFunc {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfFunc : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfFunc : ℕ) : V)] = impChainV LAct (vecOf row_eqOfFunc_as) row_eqOfFunc_c := by
  have this : rowM tbl.[((lIdx_eqOfFunc : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfFunc : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfFuncB⌝ :=
    h.2 lIdx_eqOfFunc (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfFunc]

lemma layoutTable_eqOfBvar {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfBvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfBvar : ℕ) : V)] = impChainV LAct (vecOf row_eqOfBvar_as) row_eqOfBvar_c := by
  have this : rowM tbl.[((lIdx_eqOfBvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfBvar : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfBvarB⌝ :=
    h.2 lIdx_eqOfBvar (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfBvar]

lemma layoutTable_eqOfFvar {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfFvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfFvar : ℕ) : V)] = impChainV LAct (vecOf row_eqOfFvar_as) row_eqOfFvar_c := by
  have this : rowM tbl.[((lIdx_eqOfFvar : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfFvar : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfFvarB⌝ :=
    h.2 lIdx_eqOfFvar (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfFvar]

lemma layoutTable_eqOfAdj {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_eqOfAdj : ℕ) : V)] = ((6 : ℕ) : V) ∧
    rowB tbl.[((lIdx_eqOfAdj : ℕ) : V)] = impChainV LAct (vecOf row_eqOfAdj_as) row_eqOfAdj_c := by
  have this : rowM tbl.[((lIdx_eqOfAdj : ℕ) : V)] = ((6 : ℕ) : V) ∧
      rowB tbl.[((lIdx_eqOfAdj : ℕ) : V)] = ⌜Semiformula.lMap emb eqOfAdjB⌝ :=
    h.2 lIdx_eqOfAdj (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_eqOfAdj]

lemma layoutTable_subsetAntisymm {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_subsetAntisymm : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_subsetAntisymm : ℕ) : V)] = impChainV LAct (vecOf row_subsetAntisymm_as) row_subsetAntisymm_c := by
  have this : rowM tbl.[((lIdx_subsetAntisymm : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_subsetAntisymm : ℕ) : V)] = ⌜Semiformula.lMap emb subsetAntisymmB⌝ :=
    h.2 lIdx_subsetAntisymm (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_subsetAntisymm]

lemma layoutTable_insertTotalC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_insertTotalC : ℕ) : V)] = ((2 : ℕ) : V) ∧
    rowB tbl.[((lIdx_insertTotalC : ℕ) : V)] = impChainV LAct (vecOf row_insertTotalC_as) (^∃ row_insertTotalC_R) := by
  have this : rowM tbl.[((lIdx_insertTotalC : ℕ) : V)] = ((2 : ℕ) : V) ∧
      rowB tbl.[((lIdx_insertTotalC : ℕ) : V)] = ⌜Semiformula.lMap emb insertTotalCB⌝ :=
    h.2 lIdx_insertTotalC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_insertTotalC]
  rfl

lemma layoutTable_memInsertSelfC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_memInsertSelfC : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_memInsertSelfC : ℕ) : V)] = impChainV LAct (vecOf row_memInsertSelfC_as) row_memInsertSelfC_c := by
  have this : rowM tbl.[((lIdx_memInsertSelfC : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_memInsertSelfC : ℕ) : V)] = ⌜Semiformula.lMap emb memInsertSelfCB⌝ :=
    h.2 lIdx_memInsertSelfC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_memInsertSelfC]

lemma layoutTable_subsetInsertC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_subsetInsertC : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_subsetInsertC : ℕ) : V)] = impChainV LAct (vecOf row_subsetInsertC_as) row_subsetInsertC_c := by
  have this : rowM tbl.[((lIdx_subsetInsertC : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_subsetInsertC : ℕ) : V)] = ⌜Semiformula.lMap emb subsetInsertCB⌝ :=
    h.2 lIdx_subsetInsertC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_subsetInsertC]

lemma layoutTable_subsetTransC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_subsetTransC : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_subsetTransC : ℕ) : V)] = impChainV LAct (vecOf row_subsetTransC_as) row_subsetTransC_c := by
  have this : rowM tbl.[((lIdx_subsetTransC : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_subsetTransC : ℕ) : V)] = ⌜Semiformula.lMap emb subsetTransCB⌝ :=
    h.2 lIdx_subsetTransC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_subsetTransC]

lemma layoutTable_subsetMemC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_subsetMemC : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_subsetMemC : ℕ) : V)] = impChainV LAct (vecOf row_subsetMemC_as) row_subsetMemC_c := by
  have this : rowM tbl.[((lIdx_subsetMemC : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_subsetMemC : ℕ) : V)] = ⌜Semiformula.lMap emb subsetMemCB⌝ :=
    h.2 lIdx_subsetMemC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_subsetMemC]

lemma layoutTable_emptySubsetC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_emptySubsetC : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_emptySubsetC : ℕ) : V)] = impChainV LAct (vecOf row_emptySubsetC_as) row_emptySubsetC_c := by
  have this : rowM tbl.[((lIdx_emptySubsetC : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_emptySubsetC : ℕ) : V)] = ⌜Semiformula.lMap emb emptySubsetCB⌝ :=
    h.2 lIdx_emptySubsetC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_emptySubsetC]

lemma layoutTable_fsetOfSubsetZeroC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_fsetOfSubsetZeroC : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_fsetOfSubsetZeroC : ℕ) : V)] = impChainV LAct (vecOf row_fsetOfSubsetZeroC_as) row_fsetOfSubsetZeroC_c := by
  have this : rowM tbl.[((lIdx_fsetOfSubsetZeroC : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_fsetOfSubsetZeroC : ℕ) : V)] = ⌜Semiformula.lMap emb fsetOfSubsetZeroCB⌝ :=
    h.2 lIdx_fsetOfSubsetZeroC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_fsetOfSubsetZeroC]

lemma layoutTable_isFormulaSetInsertC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_isFormulaSetInsertC : ℕ) : V)] = ((3 : ℕ) : V) ∧
    rowB tbl.[((lIdx_isFormulaSetInsertC : ℕ) : V)] = impChainV LAct (vecOf row_isFormulaSetInsertC_as) row_isFormulaSetInsertC_c := by
  have this : rowM tbl.[((lIdx_isFormulaSetInsertC : ℕ) : V)] = ((3 : ℕ) : V) ∧
      rowB tbl.[((lIdx_isFormulaSetInsertC : ℕ) : V)] = ⌜Semiformula.lMap emb isFormulaSetInsertCB⌝ :=
    h.2 lIdx_isFormulaSetInsertC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_isFormulaSetInsertC]

lemma layoutTable_fsetSigmaPiC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_fsetSigmaPiC : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_fsetSigmaPiC : ℕ) : V)] = impChainV LAct (vecOf row_fsetSigmaPiC_as) row_fsetSigmaPiC_c := by
  have this : rowM tbl.[((lIdx_fsetSigmaPiC : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_fsetSigmaPiC : ℕ) : V)] = ⌜Semiformula.lMap emb fsetSigmaPiCB⌝ :=
    h.2 lIdx_fsetSigmaPiC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_fsetSigmaPiC]

lemma layoutTable_setLenTotalC {tbl : V} (h : LayoutTable tbl) :
    rowM tbl.[((lIdx_setLenTotalC : ℕ) : V)] = ((1 : ℕ) : V) ∧
    rowB tbl.[((lIdx_setLenTotalC : ℕ) : V)] = impChainV LAct (vecOf row_setLenTotalC_as) (^∃ row_setLenTotalC_R) := by
  have this : rowM tbl.[((lIdx_setLenTotalC : ℕ) : V)] = ((1 : ℕ) : V) ∧
      rowB tbl.[((lIdx_setLenTotalC : ℕ) : V)] = ⌜Semiformula.lMap emb setLenTotalCB⌝ :=
    h.2 lIdx_setLenTotalC (Nat.lt_of_sub_eq_succ rfl)
  refine ⟨this.1, ?_⟩
  rw [impChainV_vecOf, this.2, quote_row_setLenTotalC]
  rfl

/-! ### The piece table -/

noncomputable def lpiece_eqTotal : V := ⟪(2 : V), vecOf row_eqTotal_as, row_eqTotal_R⟫
noncomputable def lpiece_eqRefl : V := ⟪(0 : V), vecOf row_eqRefl_as, row_eqRefl_c⟫
noncomputable def lpiece_eqSymm : V := ⟪(0 : V), vecOf row_eqSymm_as, row_eqSymm_c⟫
noncomputable def lpiece_eqTrans : V := ⟪(0 : V), vecOf row_eqTrans_as, row_eqTrans_c⟫
noncomputable def lpiece_congPi : V := ⟪(0 : V), vecOf row_congPi_as, row_congPi_c⟫
noncomputable def lpiece_congTPi : V := ⟪(0 : V), vecOf row_congTPi_as, row_congTPi_c⟫
noncomputable def lpiece_congTvPi : V := ⟪(0 : V), vecOf row_congTvPi_as, row_congTvPi_c⟫
noncomputable def lpiece_congUtvPi : V := ⟪(0 : V), vecOf row_congUtvPi_as, row_congUtvPi_c⟫
noncomputable def lpiece_congAnd : V := ⟪(0 : V), vecOf row_congAnd_as, row_congAnd_c⟫
noncomputable def lpiece_congOr : V := ⟪(0 : V), vecOf row_congOr_as, row_congOr_c⟫
noncomputable def lpiece_congAll : V := ⟪(0 : V), vecOf row_congAll_as, row_congAll_c⟫
noncomputable def lpiece_congExs : V := ⟪(0 : V), vecOf row_congExs_as, row_congExs_c⟫
noncomputable def lpiece_congRel : V := ⟪(0 : V), vecOf row_congRel_as, row_congRel_c⟫
noncomputable def lpiece_congNRel : V := ⟪(0 : V), vecOf row_congNRel_as, row_congNRel_c⟫
noncomputable def lpiece_congVerum : V := ⟪(0 : V), vecOf row_congVerum_as, row_congVerum_c⟫
noncomputable def lpiece_congFalsum : V := ⟪(0 : V), vecOf row_congFalsum_as, row_congFalsum_c⟫
noncomputable def lpiece_congFunc : V := ⟪(0 : V), vecOf row_congFunc_as, row_congFunc_c⟫
noncomputable def lpiece_congBvar : V := ⟪(0 : V), vecOf row_congBvar_as, row_congBvar_c⟫
noncomputable def lpiece_congFvar : V := ⟪(0 : V), vecOf row_congFvar_as, row_congFvar_c⟫
noncomputable def lpiece_congAdj : V := ⟪(0 : V), vecOf row_congAdj_as, row_congAdj_c⟫
noncomputable def lpiece_congLen : V := ⟪(0 : V), vecOf row_congLen_as, row_congLen_c⟫
noncomputable def lpiece_congTLen : V := ⟪(0 : V), vecOf row_congTLen_as, row_congTLen_c⟫
noncomputable def lpiece_congMem : V := ⟪(0 : V), vecOf row_congMem_as, row_congMem_c⟫
noncomputable def lpiece_eqOfAnd : V := ⟪(0 : V), vecOf row_eqOfAnd_as, row_eqOfAnd_c⟫
noncomputable def lpiece_eqOfOr : V := ⟪(0 : V), vecOf row_eqOfOr_as, row_eqOfOr_c⟫
noncomputable def lpiece_eqOfAll : V := ⟪(0 : V), vecOf row_eqOfAll_as, row_eqOfAll_c⟫
noncomputable def lpiece_eqOfExs : V := ⟪(0 : V), vecOf row_eqOfExs_as, row_eqOfExs_c⟫
noncomputable def lpiece_eqOfRel : V := ⟪(0 : V), vecOf row_eqOfRel_as, row_eqOfRel_c⟫
noncomputable def lpiece_eqOfNRel : V := ⟪(0 : V), vecOf row_eqOfNRel_as, row_eqOfNRel_c⟫
noncomputable def lpiece_eqOfVerum : V := ⟪(0 : V), vecOf row_eqOfVerum_as, row_eqOfVerum_c⟫
noncomputable def lpiece_eqOfFalsum : V := ⟪(0 : V), vecOf row_eqOfFalsum_as, row_eqOfFalsum_c⟫
noncomputable def lpiece_eqOfFunc : V := ⟪(0 : V), vecOf row_eqOfFunc_as, row_eqOfFunc_c⟫
noncomputable def lpiece_eqOfBvar : V := ⟪(0 : V), vecOf row_eqOfBvar_as, row_eqOfBvar_c⟫
noncomputable def lpiece_eqOfFvar : V := ⟪(0 : V), vecOf row_eqOfFvar_as, row_eqOfFvar_c⟫
noncomputable def lpiece_eqOfAdj : V := ⟪(0 : V), vecOf row_eqOfAdj_as, row_eqOfAdj_c⟫
noncomputable def lpiece_subsetAntisymm : V := ⟪(0 : V), vecOf row_subsetAntisymm_as, row_subsetAntisymm_c⟫
noncomputable def lpiece_insertTotalC : V := ⟪(2 : V), vecOf row_insertTotalC_as, row_insertTotalC_R⟫
noncomputable def lpiece_memInsertSelfC : V := ⟪(0 : V), vecOf row_memInsertSelfC_as, row_memInsertSelfC_c⟫
noncomputable def lpiece_subsetInsertC : V := ⟪(0 : V), vecOf row_subsetInsertC_as, row_subsetInsertC_c⟫
noncomputable def lpiece_subsetTransC : V := ⟪(0 : V), vecOf row_subsetTransC_as, row_subsetTransC_c⟫
noncomputable def lpiece_subsetMemC : V := ⟪(0 : V), vecOf row_subsetMemC_as, row_subsetMemC_c⟫
noncomputable def lpiece_emptySubsetC : V := ⟪(0 : V), vecOf row_emptySubsetC_as, row_emptySubsetC_c⟫
noncomputable def lpiece_fsetOfSubsetZeroC : V := ⟪(0 : V), vecOf row_fsetOfSubsetZeroC_as, row_fsetOfSubsetZeroC_c⟫
noncomputable def lpiece_isFormulaSetInsertC : V := ⟪(0 : V), vecOf row_isFormulaSetInsertC_as, row_isFormulaSetInsertC_c⟫
noncomputable def lpiece_fsetSigmaPiC : V := ⟪(0 : V), vecOf row_fsetSigmaPiC_as, row_fsetSigmaPiC_c⟫
noncomputable def lpiece_setLenTotalC : V := ⟪(2 : V), vecOf row_setLenTotalC_as, row_setLenTotalC_R⟫

/-- The extra pieces in index order. -/
noncomputable def layoutExtraPieceList : List V := [
  lpiece_eqTotal,
  lpiece_eqRefl,
  lpiece_eqSymm,
  lpiece_eqTrans,
  lpiece_congPi,
  lpiece_congTPi,
  lpiece_congTvPi,
  lpiece_congUtvPi,
  lpiece_congAnd,
  lpiece_congOr,
  lpiece_congAll,
  lpiece_congExs,
  lpiece_congRel,
  lpiece_congNRel,
  lpiece_congVerum,
  lpiece_congFalsum,
  lpiece_congFunc,
  lpiece_congBvar,
  lpiece_congFvar,
  lpiece_congAdj,
  lpiece_congLen,
  lpiece_congTLen,
  lpiece_congMem,
  lpiece_eqOfAnd,
  lpiece_eqOfOr,
  lpiece_eqOfAll,
  lpiece_eqOfExs,
  lpiece_eqOfRel,
  lpiece_eqOfNRel,
  lpiece_eqOfVerum,
  lpiece_eqOfFalsum,
  lpiece_eqOfFunc,
  lpiece_eqOfBvar,
  lpiece_eqOfFvar,
  lpiece_eqOfAdj,
  lpiece_subsetAntisymm,
  lpiece_insertTotalC,
  lpiece_memInsertSelfC,
  lpiece_subsetInsertC,
  lpiece_subsetTransC,
  lpiece_subsetMemC,
  lpiece_emptySubsetC,
  lpiece_fsetOfSubsetZeroC,
  lpiece_isFormulaSetInsertC,
  lpiece_fsetSigmaPiC,
  lpiece_setLenTotalC
]

/-- **The piece table of the layout producers** (a closed V-generic term extending `walkPieces`). -/
noncomputable def layoutPieces : V := vecOf (walkPieceList ++ layoutExtraPieceList)

lemma layoutPieces_nth_lt (i : ℕ) (hi : i < walkRowCount) : (layoutPieces : V).[(i : V)] = (walkPieces : V).[(i : V)] := by
  unfold layoutPieces walkPieces
  have h1 : i < (walkPieceList ++ layoutExtraPieceList : List V).length := by
    rw [show (walkPieceList ++ layoutExtraPieceList : List V).length = layoutRowCount from rfl]
    exact lt_of_lt_of_le hi (by decide)
  have h2 : i < (walkPieceList : List V).length := by
    rw [show (walkPieceList : List V).length = walkRowCount from rfl]; exact hi
  rw [nth_vecOf _ i h1, nth_vecOf _ i h2]
  exact List.getElem_append_left h2

/-- A walk step read from the layout pieces is the walk's step. -/
lemma mkStep_layoutPieces_lt (i : ℕ) (hi : i < walkRowCount) (ev : V) :
    mkStep layoutPieces (i : V) ev = mkStep walkPieces (i : V) ev := by
  rw [mkStep, mkStep, layoutPieces_nth_lt i hi]

lemma layoutPieces_eqTotal : (layoutPieces : V).[((lIdx_eqTotal : ℕ) : V)] = lpiece_eqTotal := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqTotal (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqTotal (ev : V) :
    mkStep layoutPieces ((lIdx_eqTotal : ℕ) : V) ev = sIntroFact ((lIdx_eqTotal : ℕ) : V) ev (vecOf row_eqTotal_as) row_eqTotal_R := by
  rw [mkStep, layoutPieces_eqTotal]
  simp [lpiece_eqTotal, sIntroFact]

lemma ltag_eqTotal {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (40 : V) ev) = 2 := by
  subst hWp
  have hk : ((lIdx_eqTotal : ℕ) : V) = (40 : V) := by simp [lIdx_eqTotal]
  rw [← hk, lmk_eqTotal]; simp

lemma layoutPieces_eqRefl : (layoutPieces : V).[((lIdx_eqRefl : ℕ) : V)] = lpiece_eqRefl := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqRefl (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqRefl (ev : V) :
    mkStep layoutPieces ((lIdx_eqRefl : ℕ) : V) ev = sUseHorn ((lIdx_eqRefl : ℕ) : V) ev (vecOf row_eqRefl_as) row_eqRefl_c := by
  rw [mkStep, layoutPieces_eqRefl]
  simp [lpiece_eqRefl, sUseHorn]

lemma ltag_eqRefl {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (41 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqRefl : ℕ) : V) = (41 : V) := by simp [lIdx_eqRefl]
  rw [← hk, lmk_eqRefl]; simp

lemma layoutPieces_eqSymm : (layoutPieces : V).[((lIdx_eqSymm : ℕ) : V)] = lpiece_eqSymm := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqSymm (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqSymm (ev : V) :
    mkStep layoutPieces ((lIdx_eqSymm : ℕ) : V) ev = sUseHorn ((lIdx_eqSymm : ℕ) : V) ev (vecOf row_eqSymm_as) row_eqSymm_c := by
  rw [mkStep, layoutPieces_eqSymm]
  simp [lpiece_eqSymm, sUseHorn]

lemma ltag_eqSymm {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (42 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqSymm : ℕ) : V) = (42 : V) := by simp [lIdx_eqSymm]
  rw [← hk, lmk_eqSymm]; simp

lemma layoutPieces_eqTrans : (layoutPieces : V).[((lIdx_eqTrans : ℕ) : V)] = lpiece_eqTrans := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqTrans (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqTrans (ev : V) :
    mkStep layoutPieces ((lIdx_eqTrans : ℕ) : V) ev = sUseHorn ((lIdx_eqTrans : ℕ) : V) ev (vecOf row_eqTrans_as) row_eqTrans_c := by
  rw [mkStep, layoutPieces_eqTrans]
  simp [lpiece_eqTrans, sUseHorn]

lemma ltag_eqTrans {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (43 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqTrans : ℕ) : V) = (43 : V) := by simp [lIdx_eqTrans]
  rw [← hk, lmk_eqTrans]; simp

lemma layoutPieces_congPi : (layoutPieces : V).[((lIdx_congPi : ℕ) : V)] = lpiece_congPi := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congPi (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congPi (ev : V) :
    mkStep layoutPieces ((lIdx_congPi : ℕ) : V) ev = sUseHorn ((lIdx_congPi : ℕ) : V) ev (vecOf row_congPi_as) row_congPi_c := by
  rw [mkStep, layoutPieces_congPi]
  simp [lpiece_congPi, sUseHorn]

lemma ltag_congPi {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (44 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congPi : ℕ) : V) = (44 : V) := by simp [lIdx_congPi]
  rw [← hk, lmk_congPi]; simp

lemma layoutPieces_congTPi : (layoutPieces : V).[((lIdx_congTPi : ℕ) : V)] = lpiece_congTPi := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congTPi (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congTPi (ev : V) :
    mkStep layoutPieces ((lIdx_congTPi : ℕ) : V) ev = sUseHorn ((lIdx_congTPi : ℕ) : V) ev (vecOf row_congTPi_as) row_congTPi_c := by
  rw [mkStep, layoutPieces_congTPi]
  simp [lpiece_congTPi, sUseHorn]

lemma ltag_congTPi {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (45 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congTPi : ℕ) : V) = (45 : V) := by simp [lIdx_congTPi]
  rw [← hk, lmk_congTPi]; simp

lemma layoutPieces_congTvPi : (layoutPieces : V).[((lIdx_congTvPi : ℕ) : V)] = lpiece_congTvPi := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congTvPi (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congTvPi (ev : V) :
    mkStep layoutPieces ((lIdx_congTvPi : ℕ) : V) ev = sUseHorn ((lIdx_congTvPi : ℕ) : V) ev (vecOf row_congTvPi_as) row_congTvPi_c := by
  rw [mkStep, layoutPieces_congTvPi]
  simp [lpiece_congTvPi, sUseHorn]

lemma ltag_congTvPi {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (46 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congTvPi : ℕ) : V) = (46 : V) := by simp [lIdx_congTvPi]
  rw [← hk, lmk_congTvPi]; simp

lemma layoutPieces_congUtvPi : (layoutPieces : V).[((lIdx_congUtvPi : ℕ) : V)] = lpiece_congUtvPi := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congUtvPi (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congUtvPi (ev : V) :
    mkStep layoutPieces ((lIdx_congUtvPi : ℕ) : V) ev = sUseHorn ((lIdx_congUtvPi : ℕ) : V) ev (vecOf row_congUtvPi_as) row_congUtvPi_c := by
  rw [mkStep, layoutPieces_congUtvPi]
  simp [lpiece_congUtvPi, sUseHorn]

lemma ltag_congUtvPi {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (47 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congUtvPi : ℕ) : V) = (47 : V) := by simp [lIdx_congUtvPi]
  rw [← hk, lmk_congUtvPi]; simp

lemma layoutPieces_congAnd : (layoutPieces : V).[((lIdx_congAnd : ℕ) : V)] = lpiece_congAnd := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congAnd (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congAnd (ev : V) :
    mkStep layoutPieces ((lIdx_congAnd : ℕ) : V) ev = sUseHorn ((lIdx_congAnd : ℕ) : V) ev (vecOf row_congAnd_as) row_congAnd_c := by
  rw [mkStep, layoutPieces_congAnd]
  simp [lpiece_congAnd, sUseHorn]

lemma ltag_congAnd {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (48 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congAnd : ℕ) : V) = (48 : V) := by simp [lIdx_congAnd]
  rw [← hk, lmk_congAnd]; simp

lemma layoutPieces_congOr : (layoutPieces : V).[((lIdx_congOr : ℕ) : V)] = lpiece_congOr := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congOr (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congOr (ev : V) :
    mkStep layoutPieces ((lIdx_congOr : ℕ) : V) ev = sUseHorn ((lIdx_congOr : ℕ) : V) ev (vecOf row_congOr_as) row_congOr_c := by
  rw [mkStep, layoutPieces_congOr]
  simp [lpiece_congOr, sUseHorn]

lemma ltag_congOr {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (49 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congOr : ℕ) : V) = (49 : V) := by simp [lIdx_congOr]
  rw [← hk, lmk_congOr]; simp

lemma layoutPieces_congAll : (layoutPieces : V).[((lIdx_congAll : ℕ) : V)] = lpiece_congAll := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congAll (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congAll (ev : V) :
    mkStep layoutPieces ((lIdx_congAll : ℕ) : V) ev = sUseHorn ((lIdx_congAll : ℕ) : V) ev (vecOf row_congAll_as) row_congAll_c := by
  rw [mkStep, layoutPieces_congAll]
  simp [lpiece_congAll, sUseHorn]

lemma ltag_congAll {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (50 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congAll : ℕ) : V) = (50 : V) := by simp [lIdx_congAll]
  rw [← hk, lmk_congAll]; simp

lemma layoutPieces_congExs : (layoutPieces : V).[((lIdx_congExs : ℕ) : V)] = lpiece_congExs := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congExs (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congExs (ev : V) :
    mkStep layoutPieces ((lIdx_congExs : ℕ) : V) ev = sUseHorn ((lIdx_congExs : ℕ) : V) ev (vecOf row_congExs_as) row_congExs_c := by
  rw [mkStep, layoutPieces_congExs]
  simp [lpiece_congExs, sUseHorn]

lemma ltag_congExs {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (51 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congExs : ℕ) : V) = (51 : V) := by simp [lIdx_congExs]
  rw [← hk, lmk_congExs]; simp

lemma layoutPieces_congRel : (layoutPieces : V).[((lIdx_congRel : ℕ) : V)] = lpiece_congRel := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congRel (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congRel (ev : V) :
    mkStep layoutPieces ((lIdx_congRel : ℕ) : V) ev = sUseHorn ((lIdx_congRel : ℕ) : V) ev (vecOf row_congRel_as) row_congRel_c := by
  rw [mkStep, layoutPieces_congRel]
  simp [lpiece_congRel, sUseHorn]

lemma ltag_congRel {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (52 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congRel : ℕ) : V) = (52 : V) := by simp [lIdx_congRel]
  rw [← hk, lmk_congRel]; simp

lemma layoutPieces_congNRel : (layoutPieces : V).[((lIdx_congNRel : ℕ) : V)] = lpiece_congNRel := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congNRel (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congNRel (ev : V) :
    mkStep layoutPieces ((lIdx_congNRel : ℕ) : V) ev = sUseHorn ((lIdx_congNRel : ℕ) : V) ev (vecOf row_congNRel_as) row_congNRel_c := by
  rw [mkStep, layoutPieces_congNRel]
  simp [lpiece_congNRel, sUseHorn]

lemma ltag_congNRel {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (53 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congNRel : ℕ) : V) = (53 : V) := by simp [lIdx_congNRel]
  rw [← hk, lmk_congNRel]; simp

lemma layoutPieces_congVerum : (layoutPieces : V).[((lIdx_congVerum : ℕ) : V)] = lpiece_congVerum := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congVerum (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congVerum (ev : V) :
    mkStep layoutPieces ((lIdx_congVerum : ℕ) : V) ev = sUseHorn ((lIdx_congVerum : ℕ) : V) ev (vecOf row_congVerum_as) row_congVerum_c := by
  rw [mkStep, layoutPieces_congVerum]
  simp [lpiece_congVerum, sUseHorn]

lemma ltag_congVerum {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (54 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congVerum : ℕ) : V) = (54 : V) := by simp [lIdx_congVerum]
  rw [← hk, lmk_congVerum]; simp

lemma layoutPieces_congFalsum : (layoutPieces : V).[((lIdx_congFalsum : ℕ) : V)] = lpiece_congFalsum := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congFalsum (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congFalsum (ev : V) :
    mkStep layoutPieces ((lIdx_congFalsum : ℕ) : V) ev = sUseHorn ((lIdx_congFalsum : ℕ) : V) ev (vecOf row_congFalsum_as) row_congFalsum_c := by
  rw [mkStep, layoutPieces_congFalsum]
  simp [lpiece_congFalsum, sUseHorn]

lemma ltag_congFalsum {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (55 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congFalsum : ℕ) : V) = (55 : V) := by simp [lIdx_congFalsum]
  rw [← hk, lmk_congFalsum]; simp

lemma layoutPieces_congFunc : (layoutPieces : V).[((lIdx_congFunc : ℕ) : V)] = lpiece_congFunc := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congFunc (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congFunc (ev : V) :
    mkStep layoutPieces ((lIdx_congFunc : ℕ) : V) ev = sUseHorn ((lIdx_congFunc : ℕ) : V) ev (vecOf row_congFunc_as) row_congFunc_c := by
  rw [mkStep, layoutPieces_congFunc]
  simp [lpiece_congFunc, sUseHorn]

lemma ltag_congFunc {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (56 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congFunc : ℕ) : V) = (56 : V) := by simp [lIdx_congFunc]
  rw [← hk, lmk_congFunc]; simp

lemma layoutPieces_congBvar : (layoutPieces : V).[((lIdx_congBvar : ℕ) : V)] = lpiece_congBvar := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congBvar (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congBvar (ev : V) :
    mkStep layoutPieces ((lIdx_congBvar : ℕ) : V) ev = sUseHorn ((lIdx_congBvar : ℕ) : V) ev (vecOf row_congBvar_as) row_congBvar_c := by
  rw [mkStep, layoutPieces_congBvar]
  simp [lpiece_congBvar, sUseHorn]

lemma ltag_congBvar {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (57 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congBvar : ℕ) : V) = (57 : V) := by simp [lIdx_congBvar]
  rw [← hk, lmk_congBvar]; simp

lemma layoutPieces_congFvar : (layoutPieces : V).[((lIdx_congFvar : ℕ) : V)] = lpiece_congFvar := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congFvar (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congFvar (ev : V) :
    mkStep layoutPieces ((lIdx_congFvar : ℕ) : V) ev = sUseHorn ((lIdx_congFvar : ℕ) : V) ev (vecOf row_congFvar_as) row_congFvar_c := by
  rw [mkStep, layoutPieces_congFvar]
  simp [lpiece_congFvar, sUseHorn]

lemma ltag_congFvar {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (58 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congFvar : ℕ) : V) = (58 : V) := by simp [lIdx_congFvar]
  rw [← hk, lmk_congFvar]; simp

lemma layoutPieces_congAdj : (layoutPieces : V).[((lIdx_congAdj : ℕ) : V)] = lpiece_congAdj := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congAdj (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congAdj (ev : V) :
    mkStep layoutPieces ((lIdx_congAdj : ℕ) : V) ev = sUseHorn ((lIdx_congAdj : ℕ) : V) ev (vecOf row_congAdj_as) row_congAdj_c := by
  rw [mkStep, layoutPieces_congAdj]
  simp [lpiece_congAdj, sUseHorn]

lemma ltag_congAdj {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (59 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congAdj : ℕ) : V) = (59 : V) := by simp [lIdx_congAdj]
  rw [← hk, lmk_congAdj]; simp

lemma layoutPieces_congLen : (layoutPieces : V).[((lIdx_congLen : ℕ) : V)] = lpiece_congLen := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congLen (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congLen (ev : V) :
    mkStep layoutPieces ((lIdx_congLen : ℕ) : V) ev = sUseHorn ((lIdx_congLen : ℕ) : V) ev (vecOf row_congLen_as) row_congLen_c := by
  rw [mkStep, layoutPieces_congLen]
  simp [lpiece_congLen, sUseHorn]

lemma ltag_congLen {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (60 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congLen : ℕ) : V) = (60 : V) := by simp [lIdx_congLen]
  rw [← hk, lmk_congLen]; simp

lemma layoutPieces_congTLen : (layoutPieces : V).[((lIdx_congTLen : ℕ) : V)] = lpiece_congTLen := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congTLen (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congTLen (ev : V) :
    mkStep layoutPieces ((lIdx_congTLen : ℕ) : V) ev = sUseHorn ((lIdx_congTLen : ℕ) : V) ev (vecOf row_congTLen_as) row_congTLen_c := by
  rw [mkStep, layoutPieces_congTLen]
  simp [lpiece_congTLen, sUseHorn]

lemma ltag_congTLen {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (61 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congTLen : ℕ) : V) = (61 : V) := by simp [lIdx_congTLen]
  rw [← hk, lmk_congTLen]; simp

lemma layoutPieces_congMem : (layoutPieces : V).[((lIdx_congMem : ℕ) : V)] = lpiece_congMem := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_congMem (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_congMem (ev : V) :
    mkStep layoutPieces ((lIdx_congMem : ℕ) : V) ev = sUseHorn ((lIdx_congMem : ℕ) : V) ev (vecOf row_congMem_as) row_congMem_c := by
  rw [mkStep, layoutPieces_congMem]
  simp [lpiece_congMem, sUseHorn]

lemma ltag_congMem {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (62 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_congMem : ℕ) : V) = (62 : V) := by simp [lIdx_congMem]
  rw [← hk, lmk_congMem]; simp

lemma layoutPieces_eqOfAnd : (layoutPieces : V).[((lIdx_eqOfAnd : ℕ) : V)] = lpiece_eqOfAnd := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfAnd (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfAnd (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfAnd : ℕ) : V) ev = sUseHorn ((lIdx_eqOfAnd : ℕ) : V) ev (vecOf row_eqOfAnd_as) row_eqOfAnd_c := by
  rw [mkStep, layoutPieces_eqOfAnd]
  simp [lpiece_eqOfAnd, sUseHorn]

lemma ltag_eqOfAnd {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (63 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfAnd : ℕ) : V) = (63 : V) := by simp [lIdx_eqOfAnd]
  rw [← hk, lmk_eqOfAnd]; simp

lemma layoutPieces_eqOfOr : (layoutPieces : V).[((lIdx_eqOfOr : ℕ) : V)] = lpiece_eqOfOr := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfOr (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfOr (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfOr : ℕ) : V) ev = sUseHorn ((lIdx_eqOfOr : ℕ) : V) ev (vecOf row_eqOfOr_as) row_eqOfOr_c := by
  rw [mkStep, layoutPieces_eqOfOr]
  simp [lpiece_eqOfOr, sUseHorn]

lemma ltag_eqOfOr {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (64 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfOr : ℕ) : V) = (64 : V) := by simp [lIdx_eqOfOr]
  rw [← hk, lmk_eqOfOr]; simp

lemma layoutPieces_eqOfAll : (layoutPieces : V).[((lIdx_eqOfAll : ℕ) : V)] = lpiece_eqOfAll := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfAll (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfAll (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfAll : ℕ) : V) ev = sUseHorn ((lIdx_eqOfAll : ℕ) : V) ev (vecOf row_eqOfAll_as) row_eqOfAll_c := by
  rw [mkStep, layoutPieces_eqOfAll]
  simp [lpiece_eqOfAll, sUseHorn]

lemma ltag_eqOfAll {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (65 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfAll : ℕ) : V) = (65 : V) := by simp [lIdx_eqOfAll]
  rw [← hk, lmk_eqOfAll]; simp

lemma layoutPieces_eqOfExs : (layoutPieces : V).[((lIdx_eqOfExs : ℕ) : V)] = lpiece_eqOfExs := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfExs (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfExs (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfExs : ℕ) : V) ev = sUseHorn ((lIdx_eqOfExs : ℕ) : V) ev (vecOf row_eqOfExs_as) row_eqOfExs_c := by
  rw [mkStep, layoutPieces_eqOfExs]
  simp [lpiece_eqOfExs, sUseHorn]

lemma ltag_eqOfExs {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (66 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfExs : ℕ) : V) = (66 : V) := by simp [lIdx_eqOfExs]
  rw [← hk, lmk_eqOfExs]; simp

lemma layoutPieces_eqOfRel : (layoutPieces : V).[((lIdx_eqOfRel : ℕ) : V)] = lpiece_eqOfRel := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfRel (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfRel (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfRel : ℕ) : V) ev = sUseHorn ((lIdx_eqOfRel : ℕ) : V) ev (vecOf row_eqOfRel_as) row_eqOfRel_c := by
  rw [mkStep, layoutPieces_eqOfRel]
  simp [lpiece_eqOfRel, sUseHorn]

lemma ltag_eqOfRel {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (67 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfRel : ℕ) : V) = (67 : V) := by simp [lIdx_eqOfRel]
  rw [← hk, lmk_eqOfRel]; simp

lemma layoutPieces_eqOfNRel : (layoutPieces : V).[((lIdx_eqOfNRel : ℕ) : V)] = lpiece_eqOfNRel := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfNRel (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfNRel (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfNRel : ℕ) : V) ev = sUseHorn ((lIdx_eqOfNRel : ℕ) : V) ev (vecOf row_eqOfNRel_as) row_eqOfNRel_c := by
  rw [mkStep, layoutPieces_eqOfNRel]
  simp [lpiece_eqOfNRel, sUseHorn]

lemma ltag_eqOfNRel {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (68 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfNRel : ℕ) : V) = (68 : V) := by simp [lIdx_eqOfNRel]
  rw [← hk, lmk_eqOfNRel]; simp

lemma layoutPieces_eqOfVerum : (layoutPieces : V).[((lIdx_eqOfVerum : ℕ) : V)] = lpiece_eqOfVerum := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfVerum (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfVerum (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfVerum : ℕ) : V) ev = sUseHorn ((lIdx_eqOfVerum : ℕ) : V) ev (vecOf row_eqOfVerum_as) row_eqOfVerum_c := by
  rw [mkStep, layoutPieces_eqOfVerum]
  simp [lpiece_eqOfVerum, sUseHorn]

lemma ltag_eqOfVerum {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (69 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfVerum : ℕ) : V) = (69 : V) := by simp [lIdx_eqOfVerum]
  rw [← hk, lmk_eqOfVerum]; simp

lemma layoutPieces_eqOfFalsum : (layoutPieces : V).[((lIdx_eqOfFalsum : ℕ) : V)] = lpiece_eqOfFalsum := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfFalsum (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfFalsum (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfFalsum : ℕ) : V) ev = sUseHorn ((lIdx_eqOfFalsum : ℕ) : V) ev (vecOf row_eqOfFalsum_as) row_eqOfFalsum_c := by
  rw [mkStep, layoutPieces_eqOfFalsum]
  simp [lpiece_eqOfFalsum, sUseHorn]

lemma ltag_eqOfFalsum {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (70 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfFalsum : ℕ) : V) = (70 : V) := by simp [lIdx_eqOfFalsum]
  rw [← hk, lmk_eqOfFalsum]; simp

lemma layoutPieces_eqOfFunc : (layoutPieces : V).[((lIdx_eqOfFunc : ℕ) : V)] = lpiece_eqOfFunc := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfFunc (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfFunc (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfFunc : ℕ) : V) ev = sUseHorn ((lIdx_eqOfFunc : ℕ) : V) ev (vecOf row_eqOfFunc_as) row_eqOfFunc_c := by
  rw [mkStep, layoutPieces_eqOfFunc]
  simp [lpiece_eqOfFunc, sUseHorn]

lemma ltag_eqOfFunc {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (71 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfFunc : ℕ) : V) = (71 : V) := by simp [lIdx_eqOfFunc]
  rw [← hk, lmk_eqOfFunc]; simp

lemma layoutPieces_eqOfBvar : (layoutPieces : V).[((lIdx_eqOfBvar : ℕ) : V)] = lpiece_eqOfBvar := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfBvar (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfBvar (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfBvar : ℕ) : V) ev = sUseHorn ((lIdx_eqOfBvar : ℕ) : V) ev (vecOf row_eqOfBvar_as) row_eqOfBvar_c := by
  rw [mkStep, layoutPieces_eqOfBvar]
  simp [lpiece_eqOfBvar, sUseHorn]

lemma ltag_eqOfBvar {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (72 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfBvar : ℕ) : V) = (72 : V) := by simp [lIdx_eqOfBvar]
  rw [← hk, lmk_eqOfBvar]; simp

lemma layoutPieces_eqOfFvar : (layoutPieces : V).[((lIdx_eqOfFvar : ℕ) : V)] = lpiece_eqOfFvar := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfFvar (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfFvar (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfFvar : ℕ) : V) ev = sUseHorn ((lIdx_eqOfFvar : ℕ) : V) ev (vecOf row_eqOfFvar_as) row_eqOfFvar_c := by
  rw [mkStep, layoutPieces_eqOfFvar]
  simp [lpiece_eqOfFvar, sUseHorn]

lemma ltag_eqOfFvar {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (73 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfFvar : ℕ) : V) = (73 : V) := by simp [lIdx_eqOfFvar]
  rw [← hk, lmk_eqOfFvar]; simp

lemma layoutPieces_eqOfAdj : (layoutPieces : V).[((lIdx_eqOfAdj : ℕ) : V)] = lpiece_eqOfAdj := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_eqOfAdj (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_eqOfAdj (ev : V) :
    mkStep layoutPieces ((lIdx_eqOfAdj : ℕ) : V) ev = sUseHorn ((lIdx_eqOfAdj : ℕ) : V) ev (vecOf row_eqOfAdj_as) row_eqOfAdj_c := by
  rw [mkStep, layoutPieces_eqOfAdj]
  simp [lpiece_eqOfAdj, sUseHorn]

lemma ltag_eqOfAdj {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (74 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_eqOfAdj : ℕ) : V) = (74 : V) := by simp [lIdx_eqOfAdj]
  rw [← hk, lmk_eqOfAdj]; simp

lemma layoutPieces_subsetAntisymm : (layoutPieces : V).[((lIdx_subsetAntisymm : ℕ) : V)] = lpiece_subsetAntisymm := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_subsetAntisymm (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_subsetAntisymm (ev : V) :
    mkStep layoutPieces ((lIdx_subsetAntisymm : ℕ) : V) ev = sUseHorn ((lIdx_subsetAntisymm : ℕ) : V) ev (vecOf row_subsetAntisymm_as) row_subsetAntisymm_c := by
  rw [mkStep, layoutPieces_subsetAntisymm]
  simp [lpiece_subsetAntisymm, sUseHorn]

lemma ltag_subsetAntisymm {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (75 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_subsetAntisymm : ℕ) : V) = (75 : V) := by simp [lIdx_subsetAntisymm]
  rw [← hk, lmk_subsetAntisymm]; simp

lemma layoutPieces_insertTotalC : (layoutPieces : V).[((lIdx_insertTotalC : ℕ) : V)] = lpiece_insertTotalC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_insertTotalC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_insertTotalC (ev : V) :
    mkStep layoutPieces ((lIdx_insertTotalC : ℕ) : V) ev = sIntroFact ((lIdx_insertTotalC : ℕ) : V) ev (vecOf row_insertTotalC_as) row_insertTotalC_R := by
  rw [mkStep, layoutPieces_insertTotalC]
  simp [lpiece_insertTotalC, sIntroFact]

lemma ltag_insertTotalC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (76 : V) ev) = 2 := by
  subst hWp
  have hk : ((lIdx_insertTotalC : ℕ) : V) = (76 : V) := by simp [lIdx_insertTotalC]
  rw [← hk, lmk_insertTotalC]; simp

lemma layoutPieces_memInsertSelfC : (layoutPieces : V).[((lIdx_memInsertSelfC : ℕ) : V)] = lpiece_memInsertSelfC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_memInsertSelfC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_memInsertSelfC (ev : V) :
    mkStep layoutPieces ((lIdx_memInsertSelfC : ℕ) : V) ev = sUseHorn ((lIdx_memInsertSelfC : ℕ) : V) ev (vecOf row_memInsertSelfC_as) row_memInsertSelfC_c := by
  rw [mkStep, layoutPieces_memInsertSelfC]
  simp [lpiece_memInsertSelfC, sUseHorn]

lemma ltag_memInsertSelfC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (77 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_memInsertSelfC : ℕ) : V) = (77 : V) := by simp [lIdx_memInsertSelfC]
  rw [← hk, lmk_memInsertSelfC]; simp

lemma layoutPieces_subsetInsertC : (layoutPieces : V).[((lIdx_subsetInsertC : ℕ) : V)] = lpiece_subsetInsertC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_subsetInsertC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_subsetInsertC (ev : V) :
    mkStep layoutPieces ((lIdx_subsetInsertC : ℕ) : V) ev = sUseHorn ((lIdx_subsetInsertC : ℕ) : V) ev (vecOf row_subsetInsertC_as) row_subsetInsertC_c := by
  rw [mkStep, layoutPieces_subsetInsertC]
  simp [lpiece_subsetInsertC, sUseHorn]

lemma ltag_subsetInsertC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (78 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_subsetInsertC : ℕ) : V) = (78 : V) := by simp [lIdx_subsetInsertC]
  rw [← hk, lmk_subsetInsertC]; simp

lemma layoutPieces_subsetTransC : (layoutPieces : V).[((lIdx_subsetTransC : ℕ) : V)] = lpiece_subsetTransC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_subsetTransC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_subsetTransC (ev : V) :
    mkStep layoutPieces ((lIdx_subsetTransC : ℕ) : V) ev = sUseHorn ((lIdx_subsetTransC : ℕ) : V) ev (vecOf row_subsetTransC_as) row_subsetTransC_c := by
  rw [mkStep, layoutPieces_subsetTransC]
  simp [lpiece_subsetTransC, sUseHorn]

lemma ltag_subsetTransC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (79 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_subsetTransC : ℕ) : V) = (79 : V) := by simp [lIdx_subsetTransC]
  rw [← hk, lmk_subsetTransC]; simp

lemma layoutPieces_subsetMemC : (layoutPieces : V).[((lIdx_subsetMemC : ℕ) : V)] = lpiece_subsetMemC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_subsetMemC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_subsetMemC (ev : V) :
    mkStep layoutPieces ((lIdx_subsetMemC : ℕ) : V) ev = sUseHorn ((lIdx_subsetMemC : ℕ) : V) ev (vecOf row_subsetMemC_as) row_subsetMemC_c := by
  rw [mkStep, layoutPieces_subsetMemC]
  simp [lpiece_subsetMemC, sUseHorn]

lemma ltag_subsetMemC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (80 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_subsetMemC : ℕ) : V) = (80 : V) := by simp [lIdx_subsetMemC]
  rw [← hk, lmk_subsetMemC]; simp

lemma layoutPieces_emptySubsetC : (layoutPieces : V).[((lIdx_emptySubsetC : ℕ) : V)] = lpiece_emptySubsetC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_emptySubsetC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_emptySubsetC (ev : V) :
    mkStep layoutPieces ((lIdx_emptySubsetC : ℕ) : V) ev = sUseHorn ((lIdx_emptySubsetC : ℕ) : V) ev (vecOf row_emptySubsetC_as) row_emptySubsetC_c := by
  rw [mkStep, layoutPieces_emptySubsetC]
  simp [lpiece_emptySubsetC, sUseHorn]

lemma ltag_emptySubsetC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (81 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_emptySubsetC : ℕ) : V) = (81 : V) := by simp [lIdx_emptySubsetC]
  rw [← hk, lmk_emptySubsetC]; simp

lemma layoutPieces_fsetOfSubsetZeroC : (layoutPieces : V).[((lIdx_fsetOfSubsetZeroC : ℕ) : V)] = lpiece_fsetOfSubsetZeroC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_fsetOfSubsetZeroC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_fsetOfSubsetZeroC (ev : V) :
    mkStep layoutPieces ((lIdx_fsetOfSubsetZeroC : ℕ) : V) ev = sUseHorn ((lIdx_fsetOfSubsetZeroC : ℕ) : V) ev (vecOf row_fsetOfSubsetZeroC_as) row_fsetOfSubsetZeroC_c := by
  rw [mkStep, layoutPieces_fsetOfSubsetZeroC]
  simp [lpiece_fsetOfSubsetZeroC, sUseHorn]

lemma ltag_fsetOfSubsetZeroC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (82 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_fsetOfSubsetZeroC : ℕ) : V) = (82 : V) := by simp [lIdx_fsetOfSubsetZeroC]
  rw [← hk, lmk_fsetOfSubsetZeroC]; simp

lemma layoutPieces_isFormulaSetInsertC : (layoutPieces : V).[((lIdx_isFormulaSetInsertC : ℕ) : V)] = lpiece_isFormulaSetInsertC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_isFormulaSetInsertC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_isFormulaSetInsertC (ev : V) :
    mkStep layoutPieces ((lIdx_isFormulaSetInsertC : ℕ) : V) ev = sUseHorn ((lIdx_isFormulaSetInsertC : ℕ) : V) ev (vecOf row_isFormulaSetInsertC_as) row_isFormulaSetInsertC_c := by
  rw [mkStep, layoutPieces_isFormulaSetInsertC]
  simp [lpiece_isFormulaSetInsertC, sUseHorn]

lemma ltag_isFormulaSetInsertC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (83 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_isFormulaSetInsertC : ℕ) : V) = (83 : V) := by simp [lIdx_isFormulaSetInsertC]
  rw [← hk, lmk_isFormulaSetInsertC]; simp

lemma layoutPieces_fsetSigmaPiC : (layoutPieces : V).[((lIdx_fsetSigmaPiC : ℕ) : V)] = lpiece_fsetSigmaPiC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_fsetSigmaPiC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_fsetSigmaPiC (ev : V) :
    mkStep layoutPieces ((lIdx_fsetSigmaPiC : ℕ) : V) ev = sUseHorn ((lIdx_fsetSigmaPiC : ℕ) : V) ev (vecOf row_fsetSigmaPiC_as) row_fsetSigmaPiC_c := by
  rw [mkStep, layoutPieces_fsetSigmaPiC]
  simp [lpiece_fsetSigmaPiC, sUseHorn]

lemma ltag_fsetSigmaPiC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (84 : V) ev) = 0 := by
  subst hWp
  have hk : ((lIdx_fsetSigmaPiC : ℕ) : V) = (84 : V) := by simp [lIdx_fsetSigmaPiC]
  rw [← hk, lmk_fsetSigmaPiC]; simp

lemma layoutPieces_setLenTotalC : (layoutPieces : V).[((lIdx_setLenTotalC : ℕ) : V)] = lpiece_setLenTotalC := by
  unfold layoutPieces
  rw [nth_vecOf _ lIdx_setLenTotalC (Nat.lt_of_sub_eq_succ rfl)]
  rfl

lemma lmk_setLenTotalC (ev : V) :
    mkStep layoutPieces ((lIdx_setLenTotalC : ℕ) : V) ev = sIntroFact ((lIdx_setLenTotalC : ℕ) : V) ev (vecOf row_setLenTotalC_as) row_setLenTotalC_R := by
  rw [mkStep, layoutPieces_setLenTotalC]
  simp [lpiece_setLenTotalC, sIntroFact]

lemma ltag_setLenTotalC {W : V} (hWp : W = layoutPieces) (ev : V) : sTag (mkStep W (85 : V) ev) = 2 := by
  subst hWp
  have hk : ((lIdx_setLenTotalC : ℕ) : V) = (85 : V) := by simp [lIdx_setLenTotalC]
  rw [← hk, lmk_setLenTotalC]; simp

end layoutTable

/-! ### The per-row applicability lemmas `lok_<row>` -/

section layoutSteps

/-- Row `eqTotal` as a step. -/
lemma lok_eqTotal {tbl N E Γ W : V} {wx : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E)  :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 40 ?[wx]) ∧ sTag (mkStep W 40 ?[wx]) = 2 ∧
    ctxAfter Γ (mkStep W 40 ?[wx]) = insert (neg LAct (eqFactB (^&((0 : ℕ) : V)) (termShift LAct wx))) (setShift LAct Γ) := by
  subst hWp
  have hk : ((lIdx_eqTotal : ℕ) : V) = (40 : V) := by simp [lIdx_eqTotal]
  have hstep := lmk_eqTotal (V := V) ?[wx]
  have hlen := layoutTable_len hL lIdx_eqTotal (by decide)
  have hrow := layoutTable_eqTotal hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_nil _⟩
  have hinst := inst_eqTotal hwx
  rw [hstep, show (?[wx] : V) = vecOf [wx] from rfl]
  refine ⟨stepOK_introFact htbl [wx] row_eqTotal_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_eqTotal_as.length = 0 from rfl]; exact_mod_cast (by decide : 0 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_nil _)
  · rw [ctxAfter_introFact [wx] row_eqTotal_as (by rw [← Nat.cast_succ]; exact isSemiformula_eqTotal_R) (fun e he ↦ (hes e he).1),
      show row_eqTotal_R = row_eqTotal_body from rfl, ← freeIter_one, hinst.2]

/-- Row `eqRefl` as a step. -/
lemma lok_eqRefl {tbl N E Γ W : V} {wx : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E)  :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 41 ?[wx]) ∧ sTag (mkStep W 41 ?[wx]) = 0 ∧
    ctxAfter Γ (mkStep W 41 ?[wx]) = insert (neg LAct (eqFactB wx wx)) Γ := by
  subst hWp
  have hk : ((lIdx_eqRefl : ℕ) : V) = (41 : V) := by simp [lIdx_eqRefl]
  have hstep := lmk_eqRefl (V := V) ?[wx]
  have hlen := layoutTable_len hL lIdx_eqRefl (by decide)
  have hrow := layoutTable_eqRefl hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_nil _⟩
  have hinst := inst_eqRefl hwx
  rw [hstep, show (?[wx] : V) = vecOf [wx] from rfl]
  refine ⟨stepOK_useHorn htbl [wx] row_eqRefl_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_eqRefl_as.length = 0 from rfl]; exact_mod_cast (by decide : 0 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_nil _)
  · rw [ctxAfter_useHorn [wx] row_eqRefl_as isSemiformula_eqRefl_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqSymm` as a step. -/
lemma lok_eqSymm {tbl N E Γ W : V} {wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wx wy) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 42 ?[wx, wy]) ∧ sTag (mkStep W 42 ?[wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 42 ?[wx, wy]) = insert (neg LAct (eqFactB wy wx)) Γ := by
  subst hWp
  have hk : ((lIdx_eqSymm : ℕ) : V) = (42 : V) := by simp [lIdx_eqSymm]
  have hstep := lmk_eqSymm (V := V) ?[wx, wy]
  have hlen := layoutTable_len hL lIdx_eqSymm (by decide)
  have hrow := layoutTable_eqSymm hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_eqSymm hwx hwy
  rw [hstep, show (?[wx, wy] : V) = vecOf [wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wy] row_eqSymm_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_eqSymm_as.length = 1 from rfl]; exact_mod_cast (by decide : 1 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_nil _⟩)
  · rw [ctxAfter_useHorn [wx, wy] row_eqSymm_as isSemiformula_eqSymm_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqTrans` as a step. -/
lemma lok_eqTrans {tbl N E Γ W : V} {wx wy wz : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hwz : IsSemiterm LAct 0 wz) (hEwz : termLen LAct wz ≤ E) (hmem0 : neg LAct (eqFactB wx wy) ∈ Γ) (hmem1 : neg LAct (eqFactB wy wz) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 43 ?[wx, wy, wz]) ∧ sTag (mkStep W 43 ?[wx, wy, wz]) = 0 ∧
    ctxAfter Γ (mkStep W 43 ?[wx, wy, wz]) = insert (neg LAct (eqFactB wx wz)) Γ := by
  subst hWp
  have hk : ((lIdx_eqTrans : ℕ) : V) = (43 : V) := by simp [lIdx_eqTrans]
  have hstep := lmk_eqTrans (V := V) ?[wx, wy, wz]
  have hlen := layoutTable_len hL lIdx_eqTrans (by decide)
  have hrow := layoutTable_eqTrans hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wy, wz], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_cons.mpr ⟨⟨hwz, hEwz⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_eqTrans hwx hwy hwz
  rw [hstep, show (?[wx, wy, wz] : V) = vecOf [wx, wy, wz] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wy, wz] row_eqTrans_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_eqTrans_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wy, wz] row_eqTrans_as isSemiformula_eqTrans_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congPi` as a step. -/
lemma lok_congPi {tbl N E Γ W : V} {wn wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwn : IsSemiterm LAct 0 wn) (hEwn : termLen LAct wn ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (piFact wn wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 44 ?[wn, wx, wy]) ∧ sTag (mkStep W 44 ?[wn, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 44 ?[wn, wx, wy]) = insert (neg LAct (sigmaFact wn wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congPi : ℕ) : V) = (44 : V) := by simp [lIdx_congPi]
  have hstep := lmk_congPi (V := V) ?[wn, wx, wy]
  have hlen := layoutTable_len hL lIdx_congPi (by decide)
  have hrow := layoutTable_congPi hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wn, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwn, hEwn⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congPi hwn hwx hwy
  rw [hstep, show (?[wn, wx, wy] : V) = vecOf [wn, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wn, wx, wy] row_congPi_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congPi_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wn, wx, wy] row_congPi_as isSemiformula_congPi_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congTPi` as a step. -/
lemma lok_congTPi {tbl N E Γ W : V} {wn wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwn : IsSemiterm LAct 0 wn) (hEwn : termLen LAct wn ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (tPiFact wn wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 45 ?[wn, wx, wy]) ∧ sTag (mkStep W 45 ?[wn, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 45 ?[wn, wx, wy]) = insert (neg LAct (tSigmaFact wn wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congTPi : ℕ) : V) = (45 : V) := by simp [lIdx_congTPi]
  have hstep := lmk_congTPi (V := V) ?[wn, wx, wy]
  have hlen := layoutTable_len hL lIdx_congTPi (by decide)
  have hrow := layoutTable_congTPi hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wn, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwn, hEwn⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congTPi hwn hwx hwy
  rw [hstep, show (?[wn, wx, wy] : V) = vecOf [wn, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wn, wx, wy] row_congTPi_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congTPi_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wn, wx, wy] row_congTPi_as isSemiformula_congTPi_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congTvPi` as a step. -/
lemma lok_congTvPi {tbl N E Γ W : V} {wk wn wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwn : IsSemiterm LAct 0 wn) (hEwn : termLen LAct wn ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (tvPiFact wk wn wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 46 ?[wk, wn, wx, wy]) ∧ sTag (mkStep W 46 ?[wk, wn, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 46 ?[wk, wn, wx, wy]) = insert (neg LAct (tvSigmaFact wk wn wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congTvPi : ℕ) : V) = (46 : V) := by simp [lIdx_congTvPi]
  have hstep := lmk_congTvPi (V := V) ?[wk, wn, wx, wy]
  have hlen := layoutTable_len hL lIdx_congTvPi (by decide)
  have hrow := layoutTable_congTvPi hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wk, wn, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwn, hEwn⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩
  have hinst := inst_congTvPi hwk hwn hwx hwy
  rw [hstep, show (?[wk, wn, wx, wy] : V) = vecOf [wk, wn, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wk, wn, wx, wy] row_congTvPi_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 4 ≤ 8))
    (by rw [show row_congTvPi_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wk, wn, wx, wy] row_congTvPi_as isSemiformula_congTvPi_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congUtvPi` as a step. -/
lemma lok_congUtvPi {tbl N E Γ W : V} {wk wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (utvPiFact wk wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 47 ?[wk, wx, wy]) ∧ sTag (mkStep W 47 ?[wk, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 47 ?[wk, wx, wy]) = insert (neg LAct (utvSigmaFact wk wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congUtvPi : ℕ) : V) = (47 : V) := by simp [lIdx_congUtvPi]
  have hstep := lmk_congUtvPi (V := V) ?[wk, wx, wy]
  have hlen := layoutTable_len hL lIdx_congUtvPi (by decide)
  have hrow := layoutTable_congUtvPi hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wk, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congUtvPi hwk hwx hwy
  rw [hstep, show (?[wk, wx, wy] : V) = vecOf [wk, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wk, wx, wy] row_congUtvPi_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congUtvPi_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wk, wx, wy] row_congUtvPi_as isSemiformula_congUtvPi_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congAnd` as a step. -/
lemma lok_congAnd {tbl N E Γ W : V} {wr wp wq wrp wpp wqp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwr : IsSemiterm LAct 0 wr) (hEwr : termLen LAct wr ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwrp : IsSemiterm LAct 0 wrp) (hEwrp : termLen LAct wrp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hmem0 : neg LAct (eqFactB wrp wr) ∈ Γ) (hmem1 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem2 : neg LAct (eqFactB wqp wq) ∈ Γ) (hmem3 : neg LAct (andFact wr wp wq) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 48 ?[wr, wp, wq, wrp, wpp, wqp]) ∧ sTag (mkStep W 48 ?[wr, wp, wq, wrp, wpp, wqp]) = 0 ∧
    ctxAfter Γ (mkStep W 48 ?[wr, wp, wq, wrp, wpp, wqp]) = insert (neg LAct (andFact wrp wpp wqp)) Γ := by
  subst hWp
  have hk : ((lIdx_congAnd : ℕ) : V) = (48 : V) := by simp [lIdx_congAnd]
  have hstep := lmk_congAnd (V := V) ?[wr, wp, wq, wrp, wpp, wqp]
  have hlen := layoutTable_len hL lIdx_congAnd (by decide)
  have hrow := layoutTable_congAnd hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wr, wp, wq, wrp, wpp, wqp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwr, hEwr⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwrp, hEwrp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congAnd hwr hwp hwq hwrp hwpp hwqp
  rw [hstep, show (?[wr, wp, wq, wrp, wpp, wqp] : V) = vecOf [wr, wp, wq, wrp, wpp, wqp] from rfl]
  refine ⟨stepOK_useHorn htbl [wr, wp, wq, wrp, wpp, wqp] row_congAnd_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congAnd_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wr, wp, wq, wrp, wpp, wqp] row_congAnd_as isSemiformula_congAnd_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congOr` as a step. -/
lemma lok_congOr {tbl N E Γ W : V} {wr wp wq wrp wpp wqp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwr : IsSemiterm LAct 0 wr) (hEwr : termLen LAct wr ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwrp : IsSemiterm LAct 0 wrp) (hEwrp : termLen LAct wrp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hmem0 : neg LAct (eqFactB wrp wr) ∈ Γ) (hmem1 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem2 : neg LAct (eqFactB wqp wq) ∈ Γ) (hmem3 : neg LAct (orFact wr wp wq) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 49 ?[wr, wp, wq, wrp, wpp, wqp]) ∧ sTag (mkStep W 49 ?[wr, wp, wq, wrp, wpp, wqp]) = 0 ∧
    ctxAfter Γ (mkStep W 49 ?[wr, wp, wq, wrp, wpp, wqp]) = insert (neg LAct (orFact wrp wpp wqp)) Γ := by
  subst hWp
  have hk : ((lIdx_congOr : ℕ) : V) = (49 : V) := by simp [lIdx_congOr]
  have hstep := lmk_congOr (V := V) ?[wr, wp, wq, wrp, wpp, wqp]
  have hlen := layoutTable_len hL lIdx_congOr (by decide)
  have hrow := layoutTable_congOr hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wr, wp, wq, wrp, wpp, wqp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwr, hEwr⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwrp, hEwrp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congOr hwr hwp hwq hwrp hwpp hwqp
  rw [hstep, show (?[wr, wp, wq, wrp, wpp, wqp] : V) = vecOf [wr, wp, wq, wrp, wpp, wqp] from rfl]
  refine ⟨stepOK_useHorn htbl [wr, wp, wq, wrp, wpp, wqp] row_congOr_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congOr_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wr, wp, wq, wrp, wpp, wqp] row_congOr_as isSemiformula_congOr_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congAll` as a step. -/
lemma lok_congAll {tbl N E Γ W : V} {wq wp wqp wpp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hmem0 : neg LAct (eqFactB wqp wq) ∈ Γ) (hmem1 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem2 : neg LAct (allFact wq wp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 50 ?[wq, wp, wqp, wpp]) ∧ sTag (mkStep W 50 ?[wq, wp, wqp, wpp]) = 0 ∧
    ctxAfter Γ (mkStep W 50 ?[wq, wp, wqp, wpp]) = insert (neg LAct (allFact wqp wpp)) Γ := by
  subst hWp
  have hk : ((lIdx_congAll : ℕ) : V) = (50 : V) := by simp [lIdx_congAll]
  have hstep := lmk_congAll (V := V) ?[wq, wp, wqp, wpp]
  have hlen := layoutTable_len hL lIdx_congAll (by decide)
  have hrow := layoutTable_congAll hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wq, wp, wqp, wpp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_nil _⟩⟩⟩⟩
  have hinst := inst_congAll hwq hwp hwqp hwpp
  rw [hstep, show (?[wq, wp, wqp, wpp] : V) = vecOf [wq, wp, wqp, wpp] from rfl]
  refine ⟨stepOK_useHorn htbl [wq, wp, wqp, wpp] row_congAll_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 4 ≤ 8))
    (by rw [show row_congAll_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wq, wp, wqp, wpp] row_congAll_as isSemiformula_congAll_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congExs` as a step. -/
lemma lok_congExs {tbl N E Γ W : V} {wq wp wqp wpp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hmem0 : neg LAct (eqFactB wqp wq) ∈ Γ) (hmem1 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem2 : neg LAct (exsFact wq wp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 51 ?[wq, wp, wqp, wpp]) ∧ sTag (mkStep W 51 ?[wq, wp, wqp, wpp]) = 0 ∧
    ctxAfter Γ (mkStep W 51 ?[wq, wp, wqp, wpp]) = insert (neg LAct (exsFact wqp wpp)) Γ := by
  subst hWp
  have hk : ((lIdx_congExs : ℕ) : V) = (51 : V) := by simp [lIdx_congExs]
  have hstep := lmk_congExs (V := V) ?[wq, wp, wqp, wpp]
  have hlen := layoutTable_len hL lIdx_congExs (by decide)
  have hrow := layoutTable_congExs hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wq, wp, wqp, wpp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_nil _⟩⟩⟩⟩
  have hinst := inst_congExs hwq hwp hwqp hwpp
  rw [hstep, show (?[wq, wp, wqp, wpp] : V) = vecOf [wq, wp, wqp, wpp] from rfl]
  refine ⟨stepOK_useHorn htbl [wq, wp, wqp, wpp] row_congExs_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 4 ≤ 8))
    (by rw [show row_congExs_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wq, wp, wqp, wpp] row_congExs_as isSemiformula_congExs_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congRel` as a step. -/
lemma lok_congRel {tbl N E Γ W : V} {wr wk wR wv wrp wvp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwr : IsSemiterm LAct 0 wr) (hEwr : termLen LAct wr ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwR : IsSemiterm LAct 0 wR) (hEwR : termLen LAct wR ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwrp : IsSemiterm LAct 0 wrp) (hEwrp : termLen LAct wrp ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hmem0 : neg LAct (eqFactB wrp wr) ∈ Γ) (hmem1 : neg LAct (eqFactB wvp wv) ∈ Γ) (hmem2 : neg LAct (relFact wr wk wR wv) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 52 ?[wr, wk, wR, wv, wrp, wvp]) ∧ sTag (mkStep W 52 ?[wr, wk, wR, wv, wrp, wvp]) = 0 ∧
    ctxAfter Γ (mkStep W 52 ?[wr, wk, wR, wv, wrp, wvp]) = insert (neg LAct (relFact wrp wk wR wvp)) Γ := by
  subst hWp
  have hk : ((lIdx_congRel : ℕ) : V) = (52 : V) := by simp [lIdx_congRel]
  have hstep := lmk_congRel (V := V) ?[wr, wk, wR, wv, wrp, wvp]
  have hlen := layoutTable_len hL lIdx_congRel (by decide)
  have hrow := layoutTable_congRel hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wr, wk, wR, wv, wrp, wvp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwr, hEwr⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwR, hEwR⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwrp, hEwrp⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congRel hwr hwk hwR hwv hwrp hwvp
  rw [hstep, show (?[wr, wk, wR, wv, wrp, wvp] : V) = vecOf [wr, wk, wR, wv, wrp, wvp] from rfl]
  refine ⟨stepOK_useHorn htbl [wr, wk, wR, wv, wrp, wvp] row_congRel_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congRel_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wr, wk, wR, wv, wrp, wvp] row_congRel_as isSemiformula_congRel_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congNRel` as a step. -/
lemma lok_congNRel {tbl N E Γ W : V} {wr wk wR wv wrp wvp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwr : IsSemiterm LAct 0 wr) (hEwr : termLen LAct wr ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwR : IsSemiterm LAct 0 wR) (hEwR : termLen LAct wR ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwrp : IsSemiterm LAct 0 wrp) (hEwrp : termLen LAct wrp ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hmem0 : neg LAct (eqFactB wrp wr) ∈ Γ) (hmem1 : neg LAct (eqFactB wvp wv) ∈ Γ) (hmem2 : neg LAct (nrelFact wr wk wR wv) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 53 ?[wr, wk, wR, wv, wrp, wvp]) ∧ sTag (mkStep W 53 ?[wr, wk, wR, wv, wrp, wvp]) = 0 ∧
    ctxAfter Γ (mkStep W 53 ?[wr, wk, wR, wv, wrp, wvp]) = insert (neg LAct (nrelFact wrp wk wR wvp)) Γ := by
  subst hWp
  have hk : ((lIdx_congNRel : ℕ) : V) = (53 : V) := by simp [lIdx_congNRel]
  have hstep := lmk_congNRel (V := V) ?[wr, wk, wR, wv, wrp, wvp]
  have hlen := layoutTable_len hL lIdx_congNRel (by decide)
  have hrow := layoutTable_congNRel hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wr, wk, wR, wv, wrp, wvp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwr, hEwr⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwR, hEwR⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwrp, hEwrp⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congNRel hwr hwk hwR hwv hwrp hwvp
  rw [hstep, show (?[wr, wk, wR, wv, wrp, wvp] : V) = vecOf [wr, wk, wR, wv, wrp, wvp] from rfl]
  refine ⟨stepOK_useHorn htbl [wr, wk, wR, wv, wrp, wvp] row_congNRel_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congNRel_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wr, wk, wR, wv, wrp, wvp] row_congNRel_as isSemiformula_congNRel_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congVerum` as a step. -/
lemma lok_congVerum {tbl N E Γ W : V} {wp wpp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hmem0 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem1 : neg LAct (verumFact wp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 54 ?[wp, wpp]) ∧ sTag (mkStep W 54 ?[wp, wpp]) = 0 ∧
    ctxAfter Γ (mkStep W 54 ?[wp, wpp]) = insert (neg LAct (verumFact wpp)) Γ := by
  subst hWp
  have hk : ((lIdx_congVerum : ℕ) : V) = (54 : V) := by simp [lIdx_congVerum]
  have hstep := lmk_congVerum (V := V) ?[wp, wpp]
  have hlen := layoutTable_len hL lIdx_congVerum (by decide)
  have hrow := layoutTable_congVerum hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wp, wpp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_congVerum hwp hwpp
  rw [hstep, show (?[wp, wpp] : V) = vecOf [wp, wpp] from rfl]
  refine ⟨stepOK_useHorn htbl [wp, wpp] row_congVerum_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_congVerum_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wp, wpp] row_congVerum_as isSemiformula_congVerum_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congFalsum` as a step. -/
lemma lok_congFalsum {tbl N E Γ W : V} {wp wpp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hmem0 : neg LAct (eqFactB wpp wp) ∈ Γ) (hmem1 : neg LAct (falsumFact wp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 55 ?[wp, wpp]) ∧ sTag (mkStep W 55 ?[wp, wpp]) = 0 ∧
    ctxAfter Γ (mkStep W 55 ?[wp, wpp]) = insert (neg LAct (falsumFact wpp)) Γ := by
  subst hWp
  have hk : ((lIdx_congFalsum : ℕ) : V) = (55 : V) := by simp [lIdx_congFalsum]
  have hstep := lmk_congFalsum (V := V) ?[wp, wpp]
  have hlen := layoutTable_len hL lIdx_congFalsum (by decide)
  have hrow := layoutTable_congFalsum hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wp, wpp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_congFalsum hwp hwpp
  rw [hstep, show (?[wp, wpp] : V) = vecOf [wp, wpp] from rfl]
  refine ⟨stepOK_useHorn htbl [wp, wpp] row_congFalsum_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_congFalsum_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wp, wpp] row_congFalsum_as isSemiformula_congFalsum_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congFunc` as a step. -/
lemma lok_congFunc {tbl N E Γ W : V} {wt wk wf wv wtp wvp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwf : IsSemiterm LAct 0 wf) (hEwf : termLen LAct wf ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hmem0 : neg LAct (eqFactB wtp wt) ∈ Γ) (hmem1 : neg LAct (eqFactB wvp wv) ∈ Γ) (hmem2 : neg LAct (funcFact wt wk wf wv) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 56 ?[wt, wk, wf, wv, wtp, wvp]) ∧ sTag (mkStep W 56 ?[wt, wk, wf, wv, wtp, wvp]) = 0 ∧
    ctxAfter Γ (mkStep W 56 ?[wt, wk, wf, wv, wtp, wvp]) = insert (neg LAct (funcFact wtp wk wf wvp)) Γ := by
  subst hWp
  have hk : ((lIdx_congFunc : ℕ) : V) = (56 : V) := by simp [lIdx_congFunc]
  have hstep := lmk_congFunc (V := V) ?[wt, wk, wf, wv, wtp, wvp]
  have hlen := layoutTable_len hL lIdx_congFunc (by decide)
  have hrow := layoutTable_congFunc hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wt, wk, wf, wv, wtp, wvp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwf, hEwf⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congFunc hwt hwk hwf hwv hwtp hwvp
  rw [hstep, show (?[wt, wk, wf, wv, wtp, wvp] : V) = vecOf [wt, wk, wf, wv, wtp, wvp] from rfl]
  refine ⟨stepOK_useHorn htbl [wt, wk, wf, wv, wtp, wvp] row_congFunc_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congFunc_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wt, wk, wf, wv, wtp, wvp] row_congFunc_as isSemiformula_congFunc_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congBvar` as a step. -/
lemma lok_congBvar {tbl N E Γ W : V} {wz wt wtp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwz : IsSemiterm LAct 0 wz) (hEwz : termLen LAct wz ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hmem0 : neg LAct (eqFactB wtp wt) ∈ Γ) (hmem1 : neg LAct (bvarFact wt wz) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 57 ?[wz, wt, wtp]) ∧ sTag (mkStep W 57 ?[wz, wt, wtp]) = 0 ∧
    ctxAfter Γ (mkStep W 57 ?[wz, wt, wtp]) = insert (neg LAct (bvarFact wtp wz)) Γ := by
  subst hWp
  have hk : ((lIdx_congBvar : ℕ) : V) = (57 : V) := by simp [lIdx_congBvar]
  have hstep := lmk_congBvar (V := V) ?[wz, wt, wtp]
  have hlen := layoutTable_len hL lIdx_congBvar (by decide)
  have hrow := layoutTable_congBvar hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wz, wt, wtp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwz, hEwz⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congBvar hwz hwt hwtp
  rw [hstep, show (?[wz, wt, wtp] : V) = vecOf [wz, wt, wtp] from rfl]
  refine ⟨stepOK_useHorn htbl [wz, wt, wtp] row_congBvar_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congBvar_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wz, wt, wtp] row_congBvar_as isSemiformula_congBvar_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congFvar` as a step. -/
lemma lok_congFvar {tbl N E Γ W : V} {wx wt wtp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hmem0 : neg LAct (eqFactB wtp wt) ∈ Γ) (hmem1 : neg LAct (fvarFact wt wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 58 ?[wx, wt, wtp]) ∧ sTag (mkStep W 58 ?[wx, wt, wtp]) = 0 ∧
    ctxAfter Γ (mkStep W 58 ?[wx, wt, wtp]) = insert (neg LAct (fvarFact wtp wx)) Γ := by
  subst hWp
  have hk : ((lIdx_congFvar : ℕ) : V) = (58 : V) := by simp [lIdx_congFvar]
  have hstep := lmk_congFvar (V := V) ?[wx, wt, wtp]
  have hlen := layoutTable_len hL lIdx_congFvar (by decide)
  have hrow := layoutTable_congFvar hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wt, wtp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congFvar hwx hwt hwtp
  rw [hstep, show (?[wx, wt, wtp] : V) = vecOf [wx, wt, wtp] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wt, wtp] row_congFvar_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congFvar_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wt, wtp] row_congFvar_as isSemiformula_congFvar_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congAdj` as a step. -/
lemma lok_congAdj {tbl N E Γ W : V} {wt wv ww wtp wvp wwp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hww : IsSemiterm LAct 0 ww) (hEww : termLen LAct ww ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hwwp : IsSemiterm LAct 0 wwp) (hEwwp : termLen LAct wwp ≤ E) (hmem0 : neg LAct (eqFactB wwp ww) ∈ Γ) (hmem1 : neg LAct (eqFactB wtp wt) ∈ Γ) (hmem2 : neg LAct (eqFactB wvp wv) ∈ Γ) (hmem3 : neg LAct (adjFact ww wt wv) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 59 ?[wt, wv, ww, wtp, wvp, wwp]) ∧ sTag (mkStep W 59 ?[wt, wv, ww, wtp, wvp, wwp]) = 0 ∧
    ctxAfter Γ (mkStep W 59 ?[wt, wv, ww, wtp, wvp, wwp]) = insert (neg LAct (adjFact wwp wtp wvp)) Γ := by
  subst hWp
  have hk : ((lIdx_congAdj : ℕ) : V) = (59 : V) := by simp [lIdx_congAdj]
  have hstep := lmk_congAdj (V := V) ?[wt, wv, ww, wtp, wvp, wwp]
  have hlen := layoutTable_len hL lIdx_congAdj (by decide)
  have hrow := layoutTable_congAdj hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wt, wv, ww, wtp, wvp, wwp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hww, hEww⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_cons.mpr ⟨⟨hwwp, hEwwp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_congAdj hwt hwv hww hwtp hwvp hwwp
  rw [hstep, show (?[wt, wv, ww, wtp, wvp, wwp] : V) = vecOf [wt, wv, ww, wtp, wvp, wwp] from rfl]
  refine ⟨stepOK_useHorn htbl [wt, wv, ww, wtp, wvp, wwp] row_congAdj_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_congAdj_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wt, wv, ww, wtp, wvp, wwp] row_congAdj_as isSemiformula_congAdj_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congLen` as a step. -/
lemma lok_congLen {tbl N E Γ W : V} {wl wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwl : IsSemiterm LAct 0 wl) (hEwl : termLen LAct wl ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (lenFact wl wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 60 ?[wl, wx, wy]) ∧ sTag (mkStep W 60 ?[wl, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 60 ?[wl, wx, wy]) = insert (neg LAct (lenFact wl wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congLen : ℕ) : V) = (60 : V) := by simp [lIdx_congLen]
  have hstep := lmk_congLen (V := V) ?[wl, wx, wy]
  have hlen := layoutTable_len hL lIdx_congLen (by decide)
  have hrow := layoutTable_congLen hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wl, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwl, hEwl⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congLen hwl hwx hwy
  rw [hstep, show (?[wl, wx, wy] : V) = vecOf [wl, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wl, wx, wy] row_congLen_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congLen_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wl, wx, wy] row_congLen_as isSemiformula_congLen_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congTLen` as a step. -/
lemma lok_congTLen {tbl N E Γ W : V} {wl wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwl : IsSemiterm LAct 0 wl) (hEwl : termLen LAct wl ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (tlenFact wl wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 61 ?[wl, wx, wy]) ∧ sTag (mkStep W 61 ?[wl, wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 61 ?[wl, wx, wy]) = insert (neg LAct (tlenFact wl wy)) Γ := by
  subst hWp
  have hk : ((lIdx_congTLen : ℕ) : V) = (61 : V) := by simp [lIdx_congTLen]
  have hstep := lmk_congTLen (V := V) ?[wl, wx, wy]
  have hlen := layoutTable_len hL lIdx_congTLen (by decide)
  have hrow := layoutTable_congTLen hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wl, wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwl, hEwl⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congTLen hwl hwx hwy
  rw [hstep, show (?[wl, wx, wy] : V) = vecOf [wl, wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wl, wx, wy] row_congTLen_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congTLen_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wl, wx, wy] row_congTLen_as isSemiformula_congTLen_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `congMem` as a step. -/
lemma lok_congMem {tbl N E Γ W : V} {wx wy ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hmem0 : neg LAct (eqFactB wy wx) ∈ Γ) (hmem1 : neg LAct (memFact wx ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 62 ?[wx, wy, ws]) ∧ sTag (mkStep W 62 ?[wx, wy, ws]) = 0 ∧
    ctxAfter Γ (mkStep W 62 ?[wx, wy, ws]) = insert (neg LAct (memFact wy ws)) Γ := by
  subst hWp
  have hk : ((lIdx_congMem : ℕ) : V) = (62 : V) := by simp [lIdx_congMem]
  have hstep := lmk_congMem (V := V) ?[wx, wy, ws]
  have hlen := layoutTable_len hL lIdx_congMem (by decide)
  have hrow := layoutTable_congMem hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wy, ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_congMem hwx hwy hws
  rw [hstep, show (?[wx, wy, ws] : V) = vecOf [wx, wy, ws] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wy, ws] row_congMem_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_congMem_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wy, ws] row_congMem_as isSemiformula_congMem_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfAnd` as a step. -/
lemma lok_eqOfAnd {tbl N E Γ W : V} {wx wp wq wpp wqp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (andFact wx wp wq) ∈ Γ) (hmem1 : neg LAct (andFact wy wpp wqp) ∈ Γ) (hmem2 : neg LAct (eqFactB wp wpp) ∈ Γ) (hmem3 : neg LAct (eqFactB wq wqp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 63 ?[wx, wp, wq, wpp, wqp, wy]) ∧ sTag (mkStep W 63 ?[wx, wp, wq, wpp, wqp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 63 ?[wx, wp, wq, wpp, wqp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfAnd : ℕ) : V) = (63 : V) := by simp [lIdx_eqOfAnd]
  have hstep := lmk_eqOfAnd (V := V) ?[wx, wp, wq, wpp, wqp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfAnd (by decide)
  have hrow := layoutTable_eqOfAnd hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wp, wq, wpp, wqp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfAnd hwx hwp hwq hwpp hwqp hwy
  rw [hstep, show (?[wx, wp, wq, wpp, wqp, wy] : V) = vecOf [wx, wp, wq, wpp, wqp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wp, wq, wpp, wqp, wy] row_eqOfAnd_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfAnd_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wp, wq, wpp, wqp, wy] row_eqOfAnd_as isSemiformula_eqOfAnd_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfOr` as a step. -/
lemma lok_eqOfOr {tbl N E Γ W : V} {wx wp wq wpp wqp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwq : IsSemiterm LAct 0 wq) (hEwq : termLen LAct wq ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwqp : IsSemiterm LAct 0 wqp) (hEwqp : termLen LAct wqp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (orFact wx wp wq) ∈ Γ) (hmem1 : neg LAct (orFact wy wpp wqp) ∈ Γ) (hmem2 : neg LAct (eqFactB wp wpp) ∈ Γ) (hmem3 : neg LAct (eqFactB wq wqp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 64 ?[wx, wp, wq, wpp, wqp, wy]) ∧ sTag (mkStep W 64 ?[wx, wp, wq, wpp, wqp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 64 ?[wx, wp, wq, wpp, wqp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfOr : ℕ) : V) = (64 : V) := by simp [lIdx_eqOfOr]
  have hstep := lmk_eqOfOr (V := V) ?[wx, wp, wq, wpp, wqp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfOr (by decide)
  have hrow := layoutTable_eqOfOr hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wp, wq, wpp, wqp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwq, hEwq⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwqp, hEwqp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfOr hwx hwp hwq hwpp hwqp hwy
  rw [hstep, show (?[wx, wp, wq, wpp, wqp, wy] : V) = vecOf [wx, wp, wq, wpp, wqp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wp, wq, wpp, wqp, wy] row_eqOfOr_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfOr_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wp, wq, wpp, wqp, wy] row_eqOfOr_as isSemiformula_eqOfOr_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfAll` as a step. -/
lemma lok_eqOfAll {tbl N E Γ W : V} {wx wp wpp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (allFact wx wp) ∈ Γ) (hmem1 : neg LAct (allFact wy wpp) ∈ Γ) (hmem2 : neg LAct (eqFactB wp wpp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 65 ?[wx, wp, wpp, wy]) ∧ sTag (mkStep W 65 ?[wx, wp, wpp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 65 ?[wx, wp, wpp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfAll : ℕ) : V) = (65 : V) := by simp [lIdx_eqOfAll]
  have hstep := lmk_eqOfAll (V := V) ?[wx, wp, wpp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfAll (by decide)
  have hrow := layoutTable_eqOfAll hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wp, wpp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩
  have hinst := inst_eqOfAll hwx hwp hwpp hwy
  rw [hstep, show (?[wx, wp, wpp, wy] : V) = vecOf [wx, wp, wpp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wp, wpp, wy] row_eqOfAll_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 4 ≤ 8))
    (by rw [show row_eqOfAll_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wp, wpp, wy] row_eqOfAll_as isSemiformula_eqOfAll_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfExs` as a step. -/
lemma lok_eqOfExs {tbl N E Γ W : V} {wx wp wpp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwpp : IsSemiterm LAct 0 wpp) (hEwpp : termLen LAct wpp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (exsFact wx wp) ∈ Γ) (hmem1 : neg LAct (exsFact wy wpp) ∈ Γ) (hmem2 : neg LAct (eqFactB wp wpp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 66 ?[wx, wp, wpp, wy]) ∧ sTag (mkStep W 66 ?[wx, wp, wpp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 66 ?[wx, wp, wpp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfExs : ℕ) : V) = (66 : V) := by simp [lIdx_eqOfExs]
  have hstep := lmk_eqOfExs (V := V) ?[wx, wp, wpp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfExs (by decide)
  have hrow := layoutTable_eqOfExs hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wp, wpp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwpp, hEwpp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩
  have hinst := inst_eqOfExs hwx hwp hwpp hwy
  rw [hstep, show (?[wx, wp, wpp, wy] : V) = vecOf [wx, wp, wpp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wp, wpp, wy] row_eqOfExs_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 4 ≤ 8))
    (by rw [show row_eqOfExs_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wp, wpp, wy] row_eqOfExs_as isSemiformula_eqOfExs_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfRel` as a step. -/
lemma lok_eqOfRel {tbl N E Γ W : V} {wx wk wR wv wvp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwR : IsSemiterm LAct 0 wR) (hEwR : termLen LAct wR ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (relFact wx wk wR wv) ∈ Γ) (hmem1 : neg LAct (relFact wy wk wR wvp) ∈ Γ) (hmem2 : neg LAct (eqFactB wv wvp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 67 ?[wx, wk, wR, wv, wvp, wy]) ∧ sTag (mkStep W 67 ?[wx, wk, wR, wv, wvp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 67 ?[wx, wk, wR, wv, wvp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfRel : ℕ) : V) = (67 : V) := by simp [lIdx_eqOfRel]
  have hstep := lmk_eqOfRel (V := V) ?[wx, wk, wR, wv, wvp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfRel (by decide)
  have hrow := layoutTable_eqOfRel hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wk, wR, wv, wvp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwR, hEwR⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfRel hwx hwk hwR hwv hwvp hwy
  rw [hstep, show (?[wx, wk, wR, wv, wvp, wy] : V) = vecOf [wx, wk, wR, wv, wvp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wk, wR, wv, wvp, wy] row_eqOfRel_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfRel_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wk, wR, wv, wvp, wy] row_eqOfRel_as isSemiformula_eqOfRel_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfNRel` as a step. -/
lemma lok_eqOfNRel {tbl N E Γ W : V} {wx wk wR wv wvp wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwR : IsSemiterm LAct 0 wR) (hEwR : termLen LAct wR ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (nrelFact wx wk wR wv) ∈ Γ) (hmem1 : neg LAct (nrelFact wy wk wR wvp) ∈ Γ) (hmem2 : neg LAct (eqFactB wv wvp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 68 ?[wx, wk, wR, wv, wvp, wy]) ∧ sTag (mkStep W 68 ?[wx, wk, wR, wv, wvp, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 68 ?[wx, wk, wR, wv, wvp, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfNRel : ℕ) : V) = (68 : V) := by simp [lIdx_eqOfNRel]
  have hstep := lmk_eqOfNRel (V := V) ?[wx, wk, wR, wv, wvp, wy]
  have hlen := layoutTable_len hL lIdx_eqOfNRel (by decide)
  have hrow := layoutTable_eqOfNRel hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wk, wR, wv, wvp, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwR, hEwR⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfNRel hwx hwk hwR hwv hwvp hwy
  rw [hstep, show (?[wx, wk, wR, wv, wvp, wy] : V) = vecOf [wx, wk, wR, wv, wvp, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wk, wR, wv, wvp, wy] row_eqOfNRel_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfNRel_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wx, wk, wR, wv, wvp, wy] row_eqOfNRel_as isSemiformula_eqOfNRel_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfVerum` as a step. -/
lemma lok_eqOfVerum {tbl N E Γ W : V} {wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (verumFact wx) ∈ Γ) (hmem1 : neg LAct (verumFact wy) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 69 ?[wx, wy]) ∧ sTag (mkStep W 69 ?[wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 69 ?[wx, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfVerum : ℕ) : V) = (69 : V) := by simp [lIdx_eqOfVerum]
  have hstep := lmk_eqOfVerum (V := V) ?[wx, wy]
  have hlen := layoutTable_len hL lIdx_eqOfVerum (by decide)
  have hrow := layoutTable_eqOfVerum hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_eqOfVerum hwx hwy
  rw [hstep, show (?[wx, wy] : V) = vecOf [wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wy] row_eqOfVerum_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_eqOfVerum_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wy] row_eqOfVerum_as isSemiformula_eqOfVerum_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfFalsum` as a step. -/
lemma lok_eqOfFalsum {tbl N E Γ W : V} {wx wy : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwy : IsSemiterm LAct 0 wy) (hEwy : termLen LAct wy ≤ E) (hmem0 : neg LAct (falsumFact wx) ∈ Γ) (hmem1 : neg LAct (falsumFact wy) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 70 ?[wx, wy]) ∧ sTag (mkStep W 70 ?[wx, wy]) = 0 ∧
    ctxAfter Γ (mkStep W 70 ?[wx, wy]) = insert (neg LAct (eqFactB wx wy)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfFalsum : ℕ) : V) = (70 : V) := by simp [lIdx_eqOfFalsum]
  have hstep := lmk_eqOfFalsum (V := V) ?[wx, wy]
  have hlen := layoutTable_len hL lIdx_eqOfFalsum (by decide)
  have hrow := layoutTable_eqOfFalsum hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wy], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwy, hEwy⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_eqOfFalsum hwx hwy
  rw [hstep, show (?[wx, wy] : V) = vecOf [wx, wy] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wy] row_eqOfFalsum_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_eqOfFalsum_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wy] row_eqOfFalsum_as isSemiformula_eqOfFalsum_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfFunc` as a step. -/
lemma lok_eqOfFunc {tbl N E Γ W : V} {wt wk wf wv wvp wtp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwk : IsSemiterm LAct 0 wk) (hEwk : termLen LAct wk ≤ E) (hwf : IsSemiterm LAct 0 wf) (hEwf : termLen LAct wf ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hmem0 : neg LAct (funcFact wt wk wf wv) ∈ Γ) (hmem1 : neg LAct (funcFact wtp wk wf wvp) ∈ Γ) (hmem2 : neg LAct (eqFactB wv wvp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 71 ?[wt, wk, wf, wv, wvp, wtp]) ∧ sTag (mkStep W 71 ?[wt, wk, wf, wv, wvp, wtp]) = 0 ∧
    ctxAfter Γ (mkStep W 71 ?[wt, wk, wf, wv, wvp, wtp]) = insert (neg LAct (eqFactB wt wtp)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfFunc : ℕ) : V) = (71 : V) := by simp [lIdx_eqOfFunc]
  have hstep := lmk_eqOfFunc (V := V) ?[wt, wk, wf, wv, wvp, wtp]
  have hlen := layoutTable_len hL lIdx_eqOfFunc (by decide)
  have hrow := layoutTable_eqOfFunc hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wt, wk, wf, wv, wvp, wtp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwk, hEwk⟩, List.forall_mem_cons.mpr ⟨⟨hwf, hEwf⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfFunc hwt hwk hwf hwv hwvp hwtp
  rw [hstep, show (?[wt, wk, wf, wv, wvp, wtp] : V) = vecOf [wt, wk, wf, wv, wvp, wtp] from rfl]
  refine ⟨stepOK_useHorn htbl [wt, wk, wf, wv, wvp, wtp] row_eqOfFunc_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfFunc_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [wt, wk, wf, wv, wvp, wtp] row_eqOfFunc_as isSemiformula_eqOfFunc_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfBvar` as a step. -/
lemma lok_eqOfBvar {tbl N E Γ W : V} {wz wt wtp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwz : IsSemiterm LAct 0 wz) (hEwz : termLen LAct wz ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hmem0 : neg LAct (bvarFact wt wz) ∈ Γ) (hmem1 : neg LAct (bvarFact wtp wz) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 72 ?[wz, wt, wtp]) ∧ sTag (mkStep W 72 ?[wz, wt, wtp]) = 0 ∧
    ctxAfter Γ (mkStep W 72 ?[wz, wt, wtp]) = insert (neg LAct (eqFactB wt wtp)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfBvar : ℕ) : V) = (72 : V) := by simp [lIdx_eqOfBvar]
  have hstep := lmk_eqOfBvar (V := V) ?[wz, wt, wtp]
  have hlen := layoutTable_len hL lIdx_eqOfBvar (by decide)
  have hrow := layoutTable_eqOfBvar hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wz, wt, wtp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwz, hEwz⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_eqOfBvar hwz hwt hwtp
  rw [hstep, show (?[wz, wt, wtp] : V) = vecOf [wz, wt, wtp] from rfl]
  refine ⟨stepOK_useHorn htbl [wz, wt, wtp] row_eqOfBvar_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_eqOfBvar_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wz, wt, wtp] row_eqOfBvar_as isSemiformula_eqOfBvar_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfFvar` as a step. -/
lemma lok_eqOfFvar {tbl N E Γ W : V} {wx wt wtp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hmem0 : neg LAct (fvarFact wt wx) ∈ Γ) (hmem1 : neg LAct (fvarFact wtp wx) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 73 ?[wx, wt, wtp]) ∧ sTag (mkStep W 73 ?[wx, wt, wtp]) = 0 ∧
    ctxAfter Γ (mkStep W 73 ?[wx, wt, wtp]) = insert (neg LAct (eqFactB wt wtp)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfFvar : ℕ) : V) = (73 : V) := by simp [lIdx_eqOfFvar]
  have hstep := lmk_eqOfFvar (V := V) ?[wx, wt, wtp]
  have hlen := layoutTable_len hL lIdx_eqOfFvar (by decide)
  have hrow := layoutTable_eqOfFvar hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, wt, wtp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_eqOfFvar hwx hwt hwtp
  rw [hstep, show (?[wx, wt, wtp] : V) = vecOf [wx, wt, wtp] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, wt, wtp] row_eqOfFvar_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_eqOfFvar_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [wx, wt, wtp] row_eqOfFvar_as isSemiformula_eqOfFvar_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `eqOfAdj` as a step. -/
lemma lok_eqOfAdj {tbl N E Γ W : V} {wt wv ww wtp wvp wwp : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwv : IsSemiterm LAct 0 wv) (hEwv : termLen LAct wv ≤ E) (hww : IsSemiterm LAct 0 ww) (hEww : termLen LAct ww ≤ E) (hwtp : IsSemiterm LAct 0 wtp) (hEwtp : termLen LAct wtp ≤ E) (hwvp : IsSemiterm LAct 0 wvp) (hEwvp : termLen LAct wvp ≤ E) (hwwp : IsSemiterm LAct 0 wwp) (hEwwp : termLen LAct wwp ≤ E) (hmem0 : neg LAct (adjFact ww wt wv) ∈ Γ) (hmem1 : neg LAct (adjFact wwp wtp wvp) ∈ Γ) (hmem2 : neg LAct (eqFactB wt wtp) ∈ Γ) (hmem3 : neg LAct (eqFactB wv wvp) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 74 ?[wt, wv, ww, wtp, wvp, wwp]) ∧ sTag (mkStep W 74 ?[wt, wv, ww, wtp, wvp, wwp]) = 0 ∧
    ctxAfter Γ (mkStep W 74 ?[wt, wv, ww, wtp, wvp, wwp]) = insert (neg LAct (eqFactB ww wwp)) Γ := by
  subst hWp
  have hk : ((lIdx_eqOfAdj : ℕ) : V) = (74 : V) := by simp [lIdx_eqOfAdj]
  have hstep := lmk_eqOfAdj (V := V) ?[wt, wv, ww, wtp, wvp, wwp]
  have hlen := layoutTable_len hL lIdx_eqOfAdj (by decide)
  have hrow := layoutTable_eqOfAdj hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wt, wv, ww, wtp, wvp, wwp], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwv, hEwv⟩, List.forall_mem_cons.mpr ⟨⟨hww, hEww⟩, List.forall_mem_cons.mpr ⟨⟨hwtp, hEwtp⟩, List.forall_mem_cons.mpr ⟨⟨hwvp, hEwvp⟩, List.forall_mem_cons.mpr ⟨⟨hwwp, hEwwp⟩, List.forall_mem_nil _⟩⟩⟩⟩⟩⟩
  have hinst := inst_eqOfAdj hwt hwv hww hwtp hwvp hwwp
  rw [hstep, show (?[wt, wv, ww, wtp, wvp, wwp] : V) = vecOf [wt, wv, ww, wtp, wvp, wwp] from rfl]
  refine ⟨stepOK_useHorn htbl [wt, wv, ww, wtp, wvp, wwp] row_eqOfAdj_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 6 ≤ 8))
    (by rw [show row_eqOfAdj_as.length = 4 from rfl]; exact_mod_cast (by decide : 4 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_cons.mpr ⟨hmem3, List.forall_mem_nil _⟩⟩⟩⟩)
  · rw [ctxAfter_useHorn [wt, wv, ww, wtp, wvp, wwp] row_eqOfAdj_as isSemiformula_eqOfAdj_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `subsetAntisymm` as a step. -/
lemma lok_subsetAntisymm {tbl N E Γ W : V} {ws wt : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hmem0 : neg LAct (subsetFact ws wt) ∈ Γ) (hmem1 : neg LAct (subsetFact wt ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 75 ?[ws, wt]) ∧ sTag (mkStep W 75 ?[ws, wt]) = 0 ∧
    ctxAfter Γ (mkStep W 75 ?[ws, wt]) = insert (neg LAct (eqFactB ws wt)) Γ := by
  subst hWp
  have hk : ((lIdx_subsetAntisymm : ℕ) : V) = (75 : V) := by simp [lIdx_subsetAntisymm]
  have hstep := lmk_subsetAntisymm (V := V) ?[ws, wt]
  have hlen := layoutTable_len hL lIdx_subsetAntisymm (by decide)
  have hrow := layoutTable_subsetAntisymm hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws, wt], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_subsetAntisymm hws hwt
  rw [hstep, show (?[ws, wt] : V) = vecOf [ws, wt] from rfl]
  refine ⟨stepOK_useHorn htbl [ws, wt] row_subsetAntisymm_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_subsetAntisymm_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [ws, wt] row_subsetAntisymm_as isSemiformula_subsetAntisymm_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `insertTotalC` as a step. -/
lemma lok_insertTotalC {tbl N E Γ W : V} {wx ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E)  :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 76 ?[wx, ws]) ∧ sTag (mkStep W 76 ?[wx, ws]) = 2 ∧
    ctxAfter Γ (mkStep W 76 ?[wx, ws]) = insert (neg LAct (insFact (^&((0 : ℕ) : V)) (termShift LAct wx) (termShift LAct ws))) (setShift LAct Γ) := by
  subst hWp
  have hk : ((lIdx_insertTotalC : ℕ) : V) = (76 : V) := by simp [lIdx_insertTotalC]
  have hstep := lmk_insertTotalC (V := V) ?[wx, ws]
  have hlen := layoutTable_len hL lIdx_insertTotalC (by decide)
  have hrow := layoutTable_insertTotalC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩⟩
  have hinst := inst_insertTotalC hwx hws
  rw [hstep, show (?[wx, ws] : V) = vecOf [wx, ws] from rfl]
  refine ⟨stepOK_introFact htbl [wx, ws] row_insertTotalC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 2 ≤ 8))
    (by rw [show row_insertTotalC_as.length = 0 from rfl]; exact_mod_cast (by decide : 0 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_nil _)
  · rw [ctxAfter_introFact [wx, ws] row_insertTotalC_as (by rw [← Nat.cast_succ]; exact isSemiformula_insertTotalC_R) (fun e he ↦ (hes e he).1),
      show row_insertTotalC_R = row_insertTotalC_body from rfl, ← freeIter_one, hinst.2]

/-- Row `memInsertSelfC` as a step. -/
lemma lok_memInsertSelfC {tbl N E Γ W : V} {wx ws wt : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hmem0 : neg LAct (insFact wt wx ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 77 ?[wx, ws, wt]) ∧ sTag (mkStep W 77 ?[wx, ws, wt]) = 0 ∧
    ctxAfter Γ (mkStep W 77 ?[wx, ws, wt]) = insert (neg LAct (memFact wx wt)) Γ := by
  subst hWp
  have hk : ((lIdx_memInsertSelfC : ℕ) : V) = (77 : V) := by simp [lIdx_memInsertSelfC]
  have hstep := lmk_memInsertSelfC (V := V) ?[wx, ws, wt]
  have hlen := layoutTable_len hL lIdx_memInsertSelfC (by decide)
  have hrow := layoutTable_memInsertSelfC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, ws, wt], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_memInsertSelfC hwx hws hwt
  rw [hstep, show (?[wx, ws, wt] : V) = vecOf [wx, ws, wt] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, ws, wt] row_memInsertSelfC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_memInsertSelfC_as.length = 1 from rfl]; exact_mod_cast (by decide : 1 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_nil _⟩)
  · rw [ctxAfter_useHorn [wx, ws, wt] row_memInsertSelfC_as isSemiformula_memInsertSelfC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `subsetInsertC` as a step. -/
lemma lok_subsetInsertC {tbl N E Γ W : V} {wx ws wt : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hmem0 : neg LAct (insFact wt wx ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 78 ?[wx, ws, wt]) ∧ sTag (mkStep W 78 ?[wx, ws, wt]) = 0 ∧
    ctxAfter Γ (mkStep W 78 ?[wx, ws, wt]) = insert (neg LAct (subsetFact ws wt)) Γ := by
  subst hWp
  have hk : ((lIdx_subsetInsertC : ℕ) : V) = (78 : V) := by simp [lIdx_subsetInsertC]
  have hstep := lmk_subsetInsertC (V := V) ?[wx, ws, wt]
  have hlen := layoutTable_len hL lIdx_subsetInsertC (by decide)
  have hrow := layoutTable_subsetInsertC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [wx, ws, wt], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_subsetInsertC hwx hws hwt
  rw [hstep, show (?[wx, ws, wt] : V) = vecOf [wx, ws, wt] from rfl]
  refine ⟨stepOK_useHorn htbl [wx, ws, wt] row_subsetInsertC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_subsetInsertC_as.length = 1 from rfl]; exact_mod_cast (by decide : 1 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_nil _⟩)
  · rw [ctxAfter_useHorn [wx, ws, wt] row_subsetInsertC_as isSemiformula_subsetInsertC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `subsetTransC` as a step. -/
lemma lok_subsetTransC {tbl N E Γ W : V} {ws wt wu : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwu : IsSemiterm LAct 0 wu) (hEwu : termLen LAct wu ≤ E) (hmem0 : neg LAct (subsetFact ws wt) ∈ Γ) (hmem1 : neg LAct (subsetFact wt wu) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 79 ?[ws, wt, wu]) ∧ sTag (mkStep W 79 ?[ws, wt, wu]) = 0 ∧
    ctxAfter Γ (mkStep W 79 ?[ws, wt, wu]) = insert (neg LAct (subsetFact ws wu)) Γ := by
  subst hWp
  have hk : ((lIdx_subsetTransC : ℕ) : V) = (79 : V) := by simp [lIdx_subsetTransC]
  have hstep := lmk_subsetTransC (V := V) ?[ws, wt, wu]
  have hlen := layoutTable_len hL lIdx_subsetTransC (by decide)
  have hrow := layoutTable_subsetTransC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws, wt, wu], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwu, hEwu⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_subsetTransC hws hwt hwu
  rw [hstep, show (?[ws, wt, wu] : V) = vecOf [ws, wt, wu] from rfl]
  refine ⟨stepOK_useHorn htbl [ws, wt, wu] row_subsetTransC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_subsetTransC_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [ws, wt, wu] row_subsetTransC_as isSemiformula_subsetTransC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `subsetMemC` as a step. -/
lemma lok_subsetMemC {tbl N E Γ W : V} {ws wt wx : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hwx : IsSemiterm LAct 0 wx) (hEwx : termLen LAct wx ≤ E) (hmem0 : neg LAct (subsetFact ws wt) ∈ Γ) (hmem1 : neg LAct (memFact wx ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 80 ?[ws, wt, wx]) ∧ sTag (mkStep W 80 ?[ws, wt, wx]) = 0 ∧
    ctxAfter Γ (mkStep W 80 ?[ws, wt, wx]) = insert (neg LAct (memFact wx wt)) Γ := by
  subst hWp
  have hk : ((lIdx_subsetMemC : ℕ) : V) = (80 : V) := by simp [lIdx_subsetMemC]
  have hstep := lmk_subsetMemC (V := V) ?[ws, wt, wx]
  have hlen := layoutTable_len hL lIdx_subsetMemC (by decide)
  have hrow := layoutTable_subsetMemC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws, wt, wx], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_cons.mpr ⟨⟨hwx, hEwx⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_subsetMemC hws hwt hwx
  rw [hstep, show (?[ws, wt, wx] : V) = vecOf [ws, wt, wx] from rfl]
  refine ⟨stepOK_useHorn htbl [ws, wt, wx] row_subsetMemC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_subsetMemC_as.length = 2 from rfl]; exact_mod_cast (by decide : 2 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_nil _⟩⟩)
  · rw [ctxAfter_useHorn [ws, wt, wx] row_subsetMemC_as isSemiformula_subsetMemC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `emptySubsetC` as a step. -/
lemma lok_emptySubsetC {tbl N E Γ W : V} {ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E)  :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 81 ?[ws]) ∧ sTag (mkStep W 81 ?[ws]) = 0 ∧
    ctxAfter Γ (mkStep W 81 ?[ws]) = insert (neg LAct (subsetFact (𝟎 : V) ws)) Γ := by
  subst hWp
  have hk : ((lIdx_emptySubsetC : ℕ) : V) = (81 : V) := by simp [lIdx_emptySubsetC]
  have hstep := lmk_emptySubsetC (V := V) ?[ws]
  have hlen := layoutTable_len hL lIdx_emptySubsetC (by decide)
  have hrow := layoutTable_emptySubsetC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩
  have hinst := inst_emptySubsetC hws
  rw [hstep, show (?[ws] : V) = vecOf [ws] from rfl]
  refine ⟨stepOK_useHorn htbl [ws] row_emptySubsetC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_emptySubsetC_as.length = 0 from rfl]; exact_mod_cast (by decide : 0 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_nil _)
  · rw [ctxAfter_useHorn [ws] row_emptySubsetC_as isSemiformula_emptySubsetC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `fsetOfSubsetZeroC` as a step. -/
lemma lok_fsetOfSubsetZeroC {tbl N E Γ W : V} {ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hmem0 : neg LAct (subsetFact ws (𝟎 : V)) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 82 ?[ws]) ∧ sTag (mkStep W 82 ?[ws]) = 0 ∧
    ctxAfter Γ (mkStep W 82 ?[ws]) = insert (neg LAct (fsetSigmaFact ws)) Γ := by
  subst hWp
  have hk : ((lIdx_fsetOfSubsetZeroC : ℕ) : V) = (82 : V) := by simp [lIdx_fsetOfSubsetZeroC]
  have hstep := lmk_fsetOfSubsetZeroC (V := V) ?[ws]
  have hlen := layoutTable_len hL lIdx_fsetOfSubsetZeroC (by decide)
  have hrow := layoutTable_fsetOfSubsetZeroC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩
  have hinst := inst_fsetOfSubsetZeroC hws
  rw [hstep, show (?[ws] : V) = vecOf [ws] from rfl]
  refine ⟨stepOK_useHorn htbl [ws] row_fsetOfSubsetZeroC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_fsetOfSubsetZeroC_as.length = 1 from rfl]; exact_mod_cast (by decide : 1 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_nil _⟩)
  · rw [ctxAfter_useHorn [ws] row_fsetOfSubsetZeroC_as isSemiformula_fsetOfSubsetZeroC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `isFormulaSetInsertC` as a step. -/
lemma lok_isFormulaSetInsertC {tbl N E Γ W : V} {ws wp wt : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hwp : IsSemiterm LAct 0 wp) (hEwp : termLen LAct wp ≤ E) (hwt : IsSemiterm LAct 0 wt) (hEwt : termLen LAct wt ≤ E) (hmem0 : neg LAct (fsetPiFact ws) ∈ Γ) (hmem1 : neg LAct (piFact (𝟎 : V) wp) ∈ Γ) (hmem2 : neg LAct (insFact wt wp ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 83 ?[ws, wp, wt]) ∧ sTag (mkStep W 83 ?[ws, wp, wt]) = 0 ∧
    ctxAfter Γ (mkStep W 83 ?[ws, wp, wt]) = insert (neg LAct (fsetSigmaFact wt)) Γ := by
  subst hWp
  have hk : ((lIdx_isFormulaSetInsertC : ℕ) : V) = (83 : V) := by simp [lIdx_isFormulaSetInsertC]
  have hstep := lmk_isFormulaSetInsertC (V := V) ?[ws, wp, wt]
  have hlen := layoutTable_len hL lIdx_isFormulaSetInsertC (by decide)
  have hrow := layoutTable_isFormulaSetInsertC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws, wp, wt], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_cons.mpr ⟨⟨hwp, hEwp⟩, List.forall_mem_cons.mpr ⟨⟨hwt, hEwt⟩, List.forall_mem_nil _⟩⟩⟩
  have hinst := inst_isFormulaSetInsertC hws hwp hwt
  rw [hstep, show (?[ws, wp, wt] : V) = vecOf [ws, wp, wt] from rfl]
  refine ⟨stepOK_useHorn htbl [ws, wp, wt] row_isFormulaSetInsertC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 3 ≤ 8))
    (by rw [show row_isFormulaSetInsertC_as.length = 3 from rfl]; exact_mod_cast (by decide : 3 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_cons.mpr ⟨hmem1, List.forall_mem_cons.mpr ⟨hmem2, List.forall_mem_nil _⟩⟩⟩)
  · rw [ctxAfter_useHorn [ws, wp, wt] row_isFormulaSetInsertC_as isSemiformula_isFormulaSetInsertC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `fsetSigmaPiC` as a step. -/
lemma lok_fsetSigmaPiC {tbl N E Γ W : V} {ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E) (hmem0 : neg LAct (fsetSigmaFact ws) ∈ Γ) :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 84 ?[ws]) ∧ sTag (mkStep W 84 ?[ws]) = 0 ∧
    ctxAfter Γ (mkStep W 84 ?[ws]) = insert (neg LAct (fsetPiFact ws)) Γ := by
  subst hWp
  have hk : ((lIdx_fsetSigmaPiC : ℕ) : V) = (84 : V) := by simp [lIdx_fsetSigmaPiC]
  have hstep := lmk_fsetSigmaPiC (V := V) ?[ws]
  have hlen := layoutTable_len hL lIdx_fsetSigmaPiC (by decide)
  have hrow := layoutTable_fsetSigmaPiC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩
  have hinst := inst_fsetSigmaPiC hws
  rw [hstep, show (?[ws] : V) = vecOf [ws] from rfl]
  refine ⟨stepOK_useHorn htbl [ws] row_fsetSigmaPiC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_fsetSigmaPiC_as.length = 1 from rfl]; exact_mod_cast (by decide : 1 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_cons.mpr ⟨hmem0, List.forall_mem_nil _⟩)
  · rw [ctxAfter_useHorn [ws] row_fsetSigmaPiC_as isSemiformula_fsetSigmaPiC_c (fun e he ↦ (hes e he).1), hinst.2]

/-- Row `setLenTotalC` as a step. -/
lemma lok_setLenTotalC {tbl N E Γ W : V} {ws : V} (htbl : TableOK tbl N) (hL : LayoutTable tbl) (hWp : W = layoutPieces)
    (hΓ : IsFormulaSet LAct Γ) (hws : IsSemiterm LAct 0 ws) (hEws : termLen LAct ws ≤ E)  :
    StepOK tbl E ((8 : ℕ) : V) Γ (mkStep W 85 ?[ws]) ∧ sTag (mkStep W 85 ?[ws]) = 2 ∧
    ctxAfter Γ (mkStep W 85 ?[ws]) = insert (neg LAct (setLenFact (^&((0 : ℕ) : V)) (termShift LAct ws))) (setShift LAct Γ) := by
  subst hWp
  have hk : ((lIdx_setLenTotalC : ℕ) : V) = (85 : V) := by simp [lIdx_setLenTotalC]
  have hstep := lmk_setLenTotalC (V := V) ?[ws]
  have hlen := layoutTable_len hL lIdx_setLenTotalC (by decide)
  have hrow := layoutTable_setLenTotalC hL
  rw [hk] at hstep hlen hrow
  have hes : ∀ e ∈ [ws], IsSemiterm LAct 0 e ∧ termLen LAct e ≤ E := List.forall_mem_cons.mpr ⟨⟨hws, hEws⟩, List.forall_mem_nil _⟩
  have hinst := inst_setLenTotalC hws
  rw [hstep, show (?[ws] : V) = vecOf [ws] from rfl]
  refine ⟨stepOK_introFact htbl [ws] row_setLenTotalC_as hΓ hlen hrow.1 hrow.2 (by exact_mod_cast (by decide : 1 ≤ 8))
    (by rw [show row_setLenTotalC_as.length = 0 from rfl]; exact_mod_cast (by decide : 0 ≤ 8)) hes ?_, by simp, ?_⟩
  · exact neg_mem_of_map hinst.1 (List.forall_mem_nil _)
  · rw [ctxAfter_introFact [ws] row_setLenTotalC_as (by rw [← Nat.cast_succ]; exact isSemiformula_setLenTotalC_R) (fun e he ↦ (hes e he).1),
      show row_setLenTotalC_R = row_setLenTotalC_body from rfl, ← freeIter_one, hinst.2]

end layoutSteps
end ArithS
